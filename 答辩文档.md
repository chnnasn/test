# Fire 丧尸生存射击游戏答辩文档

> **用途**：毕业答辩、项目展示与评审问答
> **项目**：Unity 2022.3.48f1 / URP / C# / 移动端触控适配
> **原则**：所有结论区分代码事实、运行实测、设计意图和待验证项

## 一、项目背景与目标

Fire 是一款单机低多边形 FPS 丧尸生存游戏。玩家在有限空间内抵御从多个传送门生成的敌人，通过射击、技能和 Buff 构筑完成多波次战斗。项目目标不是只实现“能开枪”，还要解决敌群追击、波次调度、成长系统、移动端输入以及对象生命周期管理等问题。

## 二、玩法与演示流程

```text
Loading 热机 → Demo 战斗 → 传送门刷怪 → 射击/技能 → 击杀获经验
      → 升级选 Buff/赌博 → 继续波次 → 全部清除胜利
                                      ↘ 生命归零失败
```

### 建议现场演示脚本

1. 启动 `Loading`，展示提示和异步加载；进入 `Demo`。
2. 使用虚拟摇杆移动，右侧触摸区域转动视角；按下瞄准、开火、换弹和奔跑按钮。
3. 观察敌人由 Portal 生成，经历 Birth、Chase、Attack 状态并追击玩家。
4. 击杀敌人获得经验；升级后展示三项 Buff 选择，并说明 Buff 会影响生命、伤害、射速、弹容量或附件。
5. 解锁后等待 Drone/IceBomb 冷却，观察其自动扫描敌群并执行 AOE；IceBomb 额外施加减速。
6. 触发赌博选项，展示它与 Buff 选择次数的关系。
7. 继续清理波次，分别演示玩家死亡失败或清除全部波次后的胜利结算。
8. 点击重新开始，验证场景、敌人、技能、粒子和投射物没有残留。

移动端输入由项目自定义 `JoyStick` 与 `ScreenTouch` 处理；`Character.OnLook` 对 Touchscreen 输入有专门规避逻辑，避免触摸和 Pointer delta 重复写入。

## 三、总体架构

```text
                    GameManager
              初始化/暂停/流场/FPS
                        │
        ┌───────────────┼───────────────┐
        ▼               ▼               ▼
   EventManager   RunTimeContext   TimeManager
   事件广播中心    Player/Character   技能与临时状态
        │               │               │
        └───────────────┼───────────────┘
                        │
       ┌────────────────┼────────────────┐
       ▼                ▼                ▼
    Player          EnemyManager      WaveManager
       │          FSM/LOD/对象池       波次/Portal
       │                │
       └────── FlowField / SpatialGrid
                        │
              ProjectilePool / 技能 / UI
```

### 结论—证据—验证—限制

| 结论 | 代码证据 | 运行验证 | 限制 |
|---|---|---|---|
| 玩家由第三方 FPS 框架驱动、自研 Player 管理成长 | `Player.cs` 持有 `Character`；`Character.cs` 管理输入、动画和武器 | 移动、瞄准、开火、换弹、Buff 后检查武器状态 | 第三方版本升级需回归 |
| 敌人使用状态机 | `EnemyStateMachine.cs`、EnemyState 目录 | 观察 Birth→Chase→Attack→Dead | 动画事件引用缺失会影响状态结束 |
| 敌人共享 FlowField | `FlowField.cs` 初始化资产并提供方向查询 | 移动玩家目标观察群体追击 | 动态障碍和大地图成本需实测 |
| 技能使用池化生成 | `Player.cs` 调用 ProjectilePool.Spawn/Release | 多轮释放和重启后检查实例数量 | 池容量与 Prefab 配置仍需验证 |

## 四、关键技术难点

### 4.1 多敌人导航

问题是为每个敌人独立寻路会重复处理相同地图障碍。当前实现以烘焙地图为基础，运行时围绕玩家目标更新共享 FlowField；敌人再叠加 SpatialGrid 邻居查询、分离力、障碍规避和 LOD 分帧更新。

- `[代码事实]` 存在 FlowField、SpatialGrid、EnemyManager 分批导航和距离分级。
- `[设计意图]` 共享方向场可减少重复寻路工作。
- `[运行实测]` 尚需固定敌人数、地图和设备后用 Profiler 测量，不能仅凭“支持 200 敌人”等注释宣称性能结论。

### 4.2 对象生命周期

敌人、弹丸、粒子、Drone 和 IceBomb 通过对象池或回收回调减少频繁创建；Enemy 的 ResetEnemy 会恢复血量、Buff、动画、粒子、碰撞和状态机。验证重点是反复释放、死亡、重启后是否存在残留对象。

### 4.3 第三方框架集成

Infima 框架负责 Character、Movement、Camera、Inventory、Weapon、Attachment 和 Animator；自研层通过公开接口、EventManager、RunTimeContext 和 BuffManager 接入生命、成长、技能和移动端按钮。这种边界减少了直接修改第三方核心代码的需要。

### 4.4 数据驱动成长

波次和 Buff 使用 ScriptableObject 配置，逻辑从资产读取而不是把所有数值写死在 UI 或流程脚本中。Buff 选择、唯一性、临时效果和技能解锁由 PlayerBuffManager 统一处理。

## 五、性能说明

当前可以陈述的内容是优化机制：对象池、静态缓冲区、SpatialGrid、sqrMagnitude、导航批次、距离 LOD、FlowField 查表和 GPU Skinning 注册/回退路径。

以下内容必须有实验记录才能在答辩中给出：

- 平均/最低 FPS、CPU/GPU Frame Time。
- 固定敌人数下的 TickActiveEnemies 和导航耗时。
- GC Alloc、池命中率、活跃对象数。
- GPU Skinning 前后 DrawCall、CPU 蒙皮和移动端功耗。

### 推荐实验设计

固定 Unity 版本、地图、分辨率、质量设置、设备和波次，分别记录不同敌人数、开启/关闭 LOD、开启/关闭对象池和 GPU 路径的 Profiler 数据，多次取均值并记录峰值。代码注释中的 133 敌、22.96ms、266KB/frame 等数字不能替代当前实验结果。

## 六、当前限制与后续方向

### 已知限制/待验证

- 动态障碍改变后 FlowField 重建策略需要单独验证。
- 高密度敌群下 CharacterController、动画和 GPU 路径的移动端表现需要实机测试。
- LOD 阈值、导航批次和 GPU Skinning 的收益依赖场景与设备。
- 第三方框架升级可能影响 Animator、Input Action、Prefab 和公开接口。
- 当前没有网络同步层，多人合作属于未来规划，不是现有功能。

### 未来方向

- 更数据导向的敌人更新或 ECS 化。
- GPU Boids 与更大规模群体模拟。
- 更多武器、敌人、地图和成长内容。
- 在明确网络架构、权威服务器和同步策略后再评估多人模式。

## 七、答辩问答

**问：为什么不用每个敌人单独寻路？**  
答：本项目敌人共享同一地图目标，FlowField 将公共路径信息集中计算，敌人只需查询方向，再用 SpatialGrid 处理个体分离；但实际收益仍需 Profiler 对比证明。

**问：SpatialGrid 解决什么问题？**  
答：它将敌人登记到空间网格，查询附近格子而非遍历全部敌人，用于分离和刷怪合法性检查。它是实现机制，不等于已经证明所有场景都是 O(1) 总成本。

**问：对象池解决什么问题？**  
答：减少弹丸、粒子和敌人频繁 Instantiate/Destroy，并统一重置 Rigidbody、粒子和状态；需要通过重复释放和 Profiler 验证其实际 GC 效果。

**问：第三方和自研如何解耦？**  
答：第三方保留 FPS 基础操作；自研 Player 通过 Character 的公开接口访问武器/附件，EventManager 转发 UI 输入，BuffManager 修改业务属性。若直接改第三方文件，应建立修改清单和升级回归测试。

**问：项目是否支持联机？**  
答：当前不支持。仓库依赖和代码中未发现常见网络框架、RPC 或同步组件；多人合作是未来方向。

**问：如何证明优化有效？**  
答：固定地图、设备、分辨率、质量和敌人数，分别开关某项机制，使用 Unity Profiler/Frame Debugger 记录帧时间、GC、DrawCall、导航耗时和活跃对象，多次重复后报告条件和结果。
