# 丧尸生存射击游戏 — 毕业设计答辩文档

> **项目名称**: 丧尸生存射击 (Zombie Survival FPS)
> **引擎**: Unity URP (Universal Render Pipeline)
> **语言**: C# 9.0 | **目标平台**: Android / iOS
> **代码量**: 自有代码 ~9,540 行 + 第三方框架 ~320 文件

---

## 一、项目概述

### 1.1 项目简介

本项目是一款基于 Unity URP 渲染管线的**移动端第三人称/第一人称丧尸生存射击游戏**。玩家在一个封闭地图中使用枪械消灭源源不断生成丧尸群，通过击杀获取经验升级，选择 Buff 增强自身能力，同时管理无人机和冰弹两种自动技能辅助战斗。游戏共设计 15 波敌人波次，通关或死亡后结算。

### 1.2 核心玩法

```
┌──────────────────────────────────────────────────┐
│                                                  │
│   射击丧尸 → 获得经验 → 升级 → 选择 Buff 强化     │
│       ↑                          ↓               │
│       │               ┌──────────────────┐       │
│       │               │ 无人机 (5s冷却)   │       │
│       └───────────────│ 冰弹   (7s冷却)   │       │
│                       │ 赌博系统          │       │
│                       └──────────────────┘       │
│                                                  │
│   15 波全部清除 → 胜利 | 玩家死亡 → 失败         │
│                                                  │
└──────────────────────────────────────────────────┘
```

### 1.3 技术栈

| 层级 | 技术选型 |
|------|---------|
| 引擎 | Unity 2021.3+ URP |
| 语言 | C# 9.0 |
| 渲染 | URP Forward Rendering + GPU ComputeShader |
| 动画 | Mecanim Animator + 骨骼动画 + GPU Skinning |
| 寻路 | 自研 FlowField 流场寻路 + SpatialGrid 空间哈希 |
| AI | FSM 有限状态机 (4状态) + Boids 群体行为 |
| 架构 | LazySingleton + EventManager + GenericProperty 响应式绑定 |
| 第三方 | Infima Games FPS 武器框架 + DOTween + Editor Toolbox |

---

## 二、系统架构设计

### 2.1 总体架构

```
┌──────────────────────────────────────────────────┐
│                   GameManager                    │
│         (启动入口、流场初始化、帧率控制)           │
└─────────┬──────────────┬──────────────┬──────────┘
          │              │              │
    ┌─────▼─────┐  ┌─────▼─────┐  ┌────▼──────┐
    │EventMgr   │  │RunTime    │  │TimeMgr    │
    │(事件中心)  │  │Context    │  │(定时器)    │
    │22个事件    │  │(依赖注入)  │  │           │
    └─────┬─────┘  └─────┬─────┘  └────┬──────┘
          │              │              │
    ┌─────▼──────────────▼──────────────▼──────────┐
    │                                               │
    │  Player ── EnemyManager ── WaveManager        │
    │    │          │               │               │
    │    │    ┌─────▼─────┐         │               │
    │    │    │ Enemy xN  │         │               │
    │    │    │  FSM 4状态 │         │               │
    │    │    └─────┬─────┘         │               │
    │    │         │               │               │
    │    └────┬────┴───────────────┘               │
    │         │                                    │
    │    ┌────▼──────────────────┐                 │
    │    │FlowField + SpatialGrid│                 │
    │    │(全局寻路+空间哈希)     │                 │
    │    └───────────────────────┘                 │
    └───────────────────────────────────────────────┘
```

### 2.2 设计模式应用

| 设计模式 | 应用位置 | 解决问题 |
|---------|---------|---------|
| **单例模式** | GameManager, EventManager, TimeManager, RunTimeContext | 全局唯一访问点 |
| **观察者模式** | EventManager (22个事件) + GenericProperty 响应式属性 | 模块间解耦通信 |
| **状态模式** | EnemyStateMachine (Birth/Chase/Attack/Dead 4状态) | 敌人行为管理 |
| **对象池模式** | ProjectilePool + EnemyManager内部池 | 减少GC和Instantiate |
| **空间哈希** | SpatialGrid (4m格子) | O(1)邻居查询 |
| **依赖注入** | RunTimeContext.Inject* 延迟绑定 | 解决循环依赖 |
| **策略模式** | FlowField vs DirectMovement | 寻路策略可切换 |
| **模板方法** | FPS框架 *Behaviour 抽象基类层 | 接口契约+多态替换 |

### 2.3 事件驱动架构

所有游戏模块通过 `EventManager` 进行通信，共设计 22 个事件：

```
战斗事件:  Fire, Reload, Aim, MoveInput, ExternalFire, ExternalRun, ExternalSprint
伤害事件:  OnAttackedAction(float), AddExper(float)
Buff事件:  TriggerBuff(int), LevelUpBuffs(string[],string[]), LevelUpBuffsFinished
流程事件:  GamePause, GameResume, BeforeDemoRestart, SettleEvent(string)
赌博事件:  RequestGambling, GamblingReady(int[],string,string,Action), 
           GamblingFinished, GamblingRoundComplete,
           GamblingGreatLuckStarted(float), GamblingGreatLuckEnded
```

---

## 三、核心技术亮点

### 3.1 自研流场寻路系统 (FlowField)

这是本项目的**核心技术亮点**之一。传统丧尸游戏使用 A* 为每个敌人寻路（200 个敌人 = 200 次 A*），CPU 开销极大。我设计并实现了一套完整的**流场寻路系统**。

**算法原理**：

```
Step 1: 编辑器预烘焙 — 将地图划分为均匀网格，Physics.CheckBox 检测障碍

Step 2: 运行时 BFS 计算 — 从玩家位置出发，BFS 计算每格到玩家的代价
         ┌───┬───┬───┬───┬───┐
         │ █ │ 1 │ 2 │ 3 │ 4 │   █ = 障碍
         ├───┼───┼───┼───┼───┤
         │ █ │10 │ ★ │14 │24 │   ★ = 玩家 (代价0)
         ├───┼───┼───┼───┼───┤
         │ → │ ↑ │ ↑ │ ← │ ← │   箭头 = 流向 (指向代价最低邻居)
         └───┴───┴───┴───┴───┘

Step 3: 敌人查询 — 每个敌人 O(1) 查表获取移动方向
         200 个敌人边际成本 ≈ 0
```

**性能优化 — 部分重建**：

| 方案 | 频率 | 开销 |
|------|------|------|
| 全量重建 | 每 5 帧 1 次 | 重置+完整BFS+全部流向 (100%) |
| 部分重建 | 每 5 帧 4 次 | 仅BFS代价减小的格子 (10~20%) |

**额外功能** — 流场查表避障：替代Physics.SphereCast，通过数组查表检测前方/左前/右前阻挡，零物理开销。

**编辑器工具** — FlowFieldBakerEditor：一键 Bake 场景障碍数据到 ScriptableObject，Scene 视图实时可视化（障碍红、目标黄、方向绿→蓝箭头、左上角图例统计）。

### 3.2 空间哈希网格 (SpatialGrid) + Boids 群体行为

**设计**：4m × 4m 网格，哈希函数 `(x << 16) ^ (z & 0xFFFF)`

```
4 个数据结构协同工作：
  Dictionary<int, List<Enemy>> _cells     — 哈希→敌人列表
  List<Enemy> _allEnemies                 — 所有登记敌人 (swap-pop O(1)删除)
  Dictionary<Enemy, int> _enemyIndices    — O(1)索引查找
  Dictionary<Enemy, int> _enemyCells      — O(1)增量更新
```

**增量更新**：仅在敌人跨格时移动列表项，隔帧执行（减半开销）。

**Boids 分离算法** — 在 EnemyChaseState 中实现完整的多力合成：
- 近距离硬推开 (平方衰减 + 上限6)
- 远距离速度分离 (反比于距离)
- 限向后分离 (最多-0.35分量)
- 振荡检测 (0.6s 内翻转>3次 → 偏向流场打破死循环)
- 切向绕行 (方向锁定0.8s + 无进度检测 → 削弱绕行)

### 3.3 AI LOD 距离分级系统

针对移动端性能限制，设计了多层 AI LOD 降频：

```
         ┌─────────────────┐
  0-15m  │ 近: 每帧/每批    │ ← 近距离敌人必须高频更新 (体验)
         ├─────────────────┤
 15-30m  │ 中: 每2帧/每2轮  │
         ├─────────────────┤
 30-50m  │ 远: 每4帧/每4轮  │
         ├─────────────────┤
  >50m   │ 极: 每8帧/每8轮  │ ← 远处敌人状态变化对体验影响极小
         └─────────────────┘

寻路分批调度: batchCount=5 → 每帧处理 1/5 敌人, 5帧完成一轮
结合 LOD: 远处敌人跳过更多轮 → 实际每帧寻路 << 敌人总数
```

### 3.4 GPU 骨骼蒙皮渲染 (GPU Skinning)

**问题**：133 个带骨骼动画的丧尸 = 133 个 Draw Call，CPU 蒙皮开销极大。

**方案**：自研 GPU Instancing 骨骼蒙皮系统

```
CPU 侧:                          GPU 侧:
LateUpdate:                      ComputeShader "CSSkinning":
  收集骨骼矩阵 → CPU Buffer         VertexCount × InstanceCount 并行蒙皮
  BoneMatrices.SetData()          输入: RestPos/Normals/UV/Weights/Bones
  Dispatch("CSSkinning")          输出: SkinnedPos/Normals/UVs
  DrawMeshInstancedIndirect()     ↓
                                 GPUSkinningLit Shader:
133 敌人 → 1 个 Draw Call         从 StructuredBuffer 读取蒙皮数据
1 次 ComputeShader Dispatch      → URP PBR 完整光照计算
```

**与 Animator 共存机制**：SMR 移到 GPU 隐藏层（不渲染但保持 enabled 以驱动骨骼）→ `AnimatorCullingMode.AlwaysAnimate` → GPU 端独立渲染。

**收益**：133 Draw Call → 1 Draw Call，CPU 蒙皮时间减少 80%+。

### 3.5 Buff 系统设计

设计了一套包含 **15 种 Buff** 的完整成长系统：

```
非唯一 Buff (可重复):         唯一 Buff (选后从池移除):
  HP回复                      瞄准镜 Scope      (减晃动)
  攻击力 AttackMultiplier      镭射 Laser       (提速)
  减伤 DamageReduction         握把 Grip        (减后座)
  吸血 LifeSteal               扩容 Magazine    (弹匣+)
  无人机技能 DroneSkillPower   技能解锁 SkillUnlock
  冰弹技能 IceBombSkillPower   背水一战 LastStand  (HP↓ Attack↑)
                              肾上腺素 Adrenaline (HP<30%触发)
                              冲刺 Sprint

多级乘法器叠加体系:
  最终伤害 = base × (1+10%×(Level-1)) × AttackMultiplier 
           × DroneSkillPower × TemporaryMultiplier
  承受伤害 = raw × (1-2%×(Level-1)) × DamageReductionMultiplier
           × TemporaryDmgReductionMultiplier
```

---

## 四、性能优化方案

### 4.1 优化策略总览

| 优化类型 | 具体方案 | 效果 |
|---------|---------|------|
| **AI LOD** | 4级距离降频 (每/2/4/8帧) | 远处敌人CPU降 50-87% |
| **分批寻路** | 每帧1/5敌人 + LOD跳过 | 寻路CPU均摊至1/5 |
| **空间网格隔帧** | RebuildAll隔帧执行 | 2ms→1ms/帧 |
| **GPU Skinning** | ComputeShader + InstancedIndirect | 133 DrawCall→1 |
| **流场部分重建** | 每5帧1次全量+4次部分 | 流场重建降 80-90% |
| **流场查表避障** | 数组查表替代Physics | 零Physics.SphereCast开销 |
| **sqrMagnitude** | 所有距离比较 | 避免sqrt() |
| **Static数组复用** | RaycastHit[8]/Collider[32] | 零GC Alloc |
| **Swap-Pop删除** | 所有List删除O(1) | 无数组搬移 |
| **对象池预热** | 首波敌人预Instantiate | 避免首波卡顿 |
| **DOTween对象复用** | Sequence.Join | 减少Tween创建 |

### 4.2 敌人追逐状态优化细节

EnemyChaseState (~520行) 是整个项目最复杂的单一组件，包含多项优化：

- **双通道更新**：攻击检测每帧执行，寻路分批调度执行
- **振荡检测**：检测方向短时间反复翻转 → 偏向流场打破死循环
- **平滑插值**：Vector3.SmoothDamp 消除分离力突变抖动
- **切向绕行**：近距离拥挤时沿玩家切线绕行+方向锁定防频繁切换
- **到达减速**：接近目标时降速防冲过头
- **贴墙滑动**：流场查表替代Physics，无射线开销

---

## 五、第三方 FPS 框架集成

### 5.1 Infima Games Low Poly Shooter Pack

采用业界成熟的 FPS 武器框架，其核心设计是**抽象基类 + 实现层**分离：

```
CharacterBehaviour (197行抽象接口) → Character (~1050行实现)
WeaponBehaviour    (213行抽象接口) → Weapon    (~730行实现)
InventoryBehaviour (52行抽象接口)  → Inventory (109行实现)
```

### 5.2 集成方式

本项目通过 **EventManager 桥接模式** 将自有系统与 FPS 框架解耦集成：

```
移动端摇杆/按钮 → EventManager → Character.SetAimingExternal/SetExternalFire等
Character 暴露 6 个 GenericProperty → PlayerUI 自动绑定更新
PlayerBuffManager → Weapon.GetAmmunitionTotal/GetRateOfFire → Buff倍率应用
```

---

## 六、项目工作量统计

### 6.1 代码规模

| 模块 | 文件数 | 代码行数 | 核心复杂度 |
|------|--------|---------|----------|
| Framework (8个管理器) | 8 | ~1,900 | ●●● |
| Enemy + EnemyState (敌人系统) | 10 | ~2,300 | ●●●●● |
| FSM (状态机) | 4 | ~150 | ●● |
| FlowField + SpatialGrid (寻路) | 3 | ~1,050 | ●●●●● |
| Player + BuffManager (玩家系统) | 6 | ~2,000 | ●●●●● |
| UI (14个面板) | 14 | ~1,400 | ●●● |
| Wave + Combat + Editor + Tool | 9 | ~600 | ●● |
| GPUSkinningLit Shader | 1 | ~140 | ●●● |
| **自有代码合计** | **~55** | **~9,540** | — |
| 第三方FPS框架 | ~91 | ~10,000+ | ●●●● |
| Editor Toolbox | ~231 | — | ●● |

### 6.2 ScriptableObject 数据资产

```
15 个波次配置  (PortalWave 0~14)
15 个 Buff 定义 (6非唯一 + 9唯一)
6 个配置资产   (EnemyBuffConfig / PlayerBuffConfig / BuffPool / PlayerLevel / TipDatabase)
1 个流场资产   (FlowField — 编辑器Bake的地图障碍数据)
```

### 6.3 预制体与资源

```
3 种丧尸 (Stand/Climb/Boom) + 对应的 AnimatorController
2 种技能预制体 (Drone/IceBomb)
1 个玩家预制体 (Player)
完整的 FPS 武器系统 (多种枪械 + 配件 + 动画)
UI 预制体 (BuffChoose/Gambling/Settle/HUD等)
```

---

## 七、技术难点与解决方案

### 7.1 难点1: 大规模敌人的实时寻路

**问题**: 200 个丧尸同时使用 A* 寻路 = 200 次 A* = CPU 爆炸。

**解决**: 自研 FlowField 流场寻路系统。所有敌人共享一个全局向量场，每个敌人 O(1) 查表。配合部分重建算法 (每5帧1次全量+4次部分)，将寻路开销降至传统方案的 1/50 以下。

### 7.2 难点2: 移动端大量骨骼动画渲染

**问题**: 133 个 SkinnedMeshRenderer = 133 Draw Call + 133 次 CPU 蒙皮。

**解决**: 自研 GPU ComputeShader 骨骼蒙皮系统。CPU 收集骨骼矩阵 → ComputeShader 并行蒙皮 → DrawMeshInstancedIndirect 单次绘制。133 DrawCall → 1 DrawCall，CPU 蒙皮时间减少 80%+。

### 7.3 难点3: 敌人群体行为与碰撞

**问题**: 大量敌人容易聚团、穿模、路径振荡。

**解决**: 多层行为控制 — SpatialGrid 空间哈希 O(1) 邻居查询 + Boids 分离力 + 硬推开 + 振荡检测与打破 + 切向绕行 + 流场查表避障 + 贴墙滑动。500+ 行代码实现完整的追逐行为。

### 7.4 难点4: 复杂Buff系统设计

**问题**: 15 种 Buff 之间存在复杂的乘法器叠加关系，且包含唯一/非唯一/临时三种生命周期。

**解决**: 设计分层属性计算链 (Level层→Buff层→Temporary层)，每种属性独立计算乘法器链，支持多临时效果堆叠。唯一 Buff 通过 HashSet 去重管理。肾上腺素通过状态机实现"获得→触发→过期"的完整生命周期。

### 7.5 难点5: 跨模块通信与循环依赖

**问题**: Player、Enemy、UI、WaveManager 等模块高度耦合。

**解决**: EventManager 集中式事件中心 (22 个事件) + RunTimeContext 延迟依赖注入 (Inject 模式) + GenericProperty 响应式数据绑定。所有模块间通信通过事件解耦，取消订阅使用安全模式防止访问已销毁对象。

---

## 八、总结与展望

### 8.1 项目成果

- 成功实现了一款**功能完整的移动端丧尸生存射击游戏**
- 自研 **FlowField 流场寻路系统** 解决大规模敌人寻路问题
- 自研 **GPU ComputeShader 骨骼蒙皮** 解决移动端渲染瓶颈
- 设计并实现**完整的 Buff 成长系统** (15种 + 3种生命周期)
- 集成第三方 FPS 框架并实现**解耦桥接**
- 实现了 **AI LOD、分批寻路、空间哈希、对象池** 等多项性能优化

### 8.2 个人技术成长

- 深入理解了**流场寻路 (BFS-based Vector Field)** 的算法原理与实现
- 掌握了 **GPU ComputeShader + StructuredBuffer + InstancedIndirect** 的 GPU 编程技术
- 实践了**设计模式**在游戏开发中的实际应用 (状态/观察者/对象池/依赖注入等)
- 学习了**第三方框架的集成与桥接**方法
- 积累了**移动端性能优化**的实战经验

### 8.3 未来改进方向

- 引入 ECS (Entity Component System) 架构进一步优化大规模敌人管理
- 实现 GPU 端 Boids 分离计算 (完全 GPU 驱动)
- 添加网络多人合作模式
- 丰富武器种类和敌人类型
- 优化 UI/UX 交互体验

---

*答辩文档 | 2026-07-15*