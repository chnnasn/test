# 丧尸生存射击游戏 — 毕业设计答辩文档

> **引擎**: Unity URP | **语言**: C# 9.0 | **平台**: Android/iOS | **文档版本**: v3.0 / 2026-07-15

---

## 一、项目概述

### 1.1 项目简介

一款基于 Unity URP 的**移动端丧尸生存射击游戏**。玩家使用枪械消灭源源不断的丧尸群，通过击杀获取经验升级，选择 Buff 增强自身，管理无人机和冰弹两种自动技能。共设计 15 波敌人波次。

### 1.2 核心玩法循环

```
射击丧尸 → 获得经验 → 升级 → 选择Buff强化
    ↑                        │
    │              ┌─────────┼─────────┐
    │              │ 无人机(5s) 冰弹(7s) 赌博 │
    │              └─────────┼─────────┘
    └────────────────────────┘
15波全清 → 胜利 | 死亡 → 失败
```

### 1.3 技术栈

| 层级 | 选型 |
|------|------|
| 引擎 | Unity 2021.3+ URP |
| 语言 | C# 9.0 |
| 渲染 | URP Forward + GPU ComputeShader |
| 寻路 | 自研 FlowField 流场 + SpatialGrid 空间哈希 |
| AI | FSM 4状态 + Boids 群体行为 |
| 架构 | LazySingleton + EventManager(22事件) + GenericProperty 响应式绑定 |
| 第三方 | Infima Games FPS框架 + DOTween |

---

## 二、系统架构

### 2.1 总体架构图

```
                   GameManager (启动/流场/FPS)
                          │
        ┌─────────────────┼─────────────────┐
        ▼                 ▼                 ▼
   EventManager      RunTimeContext      TimeManager
   (22事件中心)       (依赖注入)          (统一定时器)
        │                 │                 │
        └─────────────────┼─────────────────┘
                          │
        ┌─────────────────┼─────────────────┐
        ▼                 ▼                 ▼
     Player          EnemyManager        WaveManager
        │            ▲       ▲               │
        │        ┌───┴───────┴───┐           │
        │        ▼               ▼           │
        │   FlowField        SpatialGrid     │
        │   (全局BFS流场)    (空间哈希)       │
        │                                   │
        └──── ProjectilePool / Drone / IceBomb
```

### 2.2 设计模式

| 模式 | 位置 | 解决问题 |
|------|------|---------|
| **单例** | GameManager/EventManager/TimeManager/RunTimeContext | 全局唯一访问 |
| **观察者** | EventManager(22事件)+GenericProperty | 模块解耦 |
| **状态** | EnemyStateMachine(4状态) | 敌人行为管理 |
| **对象池** | ProjectilePool+EnemyManager内池 | 减少GC |
| **空间哈希** | SpatialGrid(4m格) | O(1)邻居查询 |
| **依赖注入** | RunTimeContext.Inject* | 循环依赖 |
| **模板方法** | FPS框架*Behaviour抽象基类 | 接口契约 |

### 2.3 事件驱动

22个事件全部通过EventManager通信:

```
战斗: Fire/Reload/Aim/MoveInput/ExternalFire/Run/Sprint
伤害: OnAttackedAction(float)/AddExper(float)
Buff: TriggerBuff(int)/LevelUpBuffs/LevelUpBuffsFinished
流程: GamePause/GameResume/BeforeDemoRestart/SettleEvent(string)
赌博: RequestGambling/GamblingReady/Finished/RoundComplete/GreatLuck
```

---

## 三、核心技术亮点

### 3.1 自研流场寻路 (FlowField)

**问题**: 200个丧尸使用A* = 200次寻路 = CPU爆炸。

**方案**: 全局BFS向量场。从玩家位置BFS计算每格的代价和流向，所有敌人O(1)查表。

```
编辑器Bake(Physics.CheckBox检测障碍)
    ↓
运行时 BFS(8方向, 正交10对角14)
    ↓
标记: cost堆 + flowDirections方向向量
    ↓
敌人查表 O(1) → 200敌人边际成本≈0
```

**部分重建优化**: 每5帧1次全量+4次部分。部分重建仅BFS代价减小的格子，耗时约全量的10-20%。

**流场查表避障**: IsDirectionBlocked / GetObstacleAvoidanceBias / ProjectDirectionByObstacle 替代Physics.SphereCast，零物理开销。

**编辑器**: FlowFieldBakerEditor一键Bake到ScriptableObject + Scene视图Gizmo可视化。

### 3.2 空间哈希 + Boids群体行为

SpatialGrid (4m格, `Hash=(x<<16)^(z&0xFFFF)`)：

4数据结构协同: Dict→List + List + Dict×2 → O(1)增量更新+邻居查询。

ChaseState中的Boids合成力:
- 硬推开(平方衰减+上限6) + 速度分离(反比距离) + 限向后(≤0.35)
- 振荡检测(0.6s翻转≥3次→偏向流场) + 切向绕行(0.8s方向锁+无进度检测)

### 3.3 AI LOD分帧

| 距离 | 状态更新 | 寻路更新 |
|------|---------|---------|
| 0-15m | 每帧 | 每批 |
| 15-30m | 每2帧 | 每2轮 |
| 30-50m | 每4帧 | 每4轮 |
| >50m | 每8帧 | 每8轮 |

结合分批寻路(batchCount=5→每帧1/5敌人)，远处敌人实际寻路调用极低。

### 3.4 GPU骨骼蒙皮 (GPU Skinning)

**问题**: 133敌=133 DrawCall + 133次CPU蒙皮。

**方案**: ComputeShader CSSkinning并行蒙皮 + DrawMeshInstancedIndirect单DrawCall + 自研GPUSkinningLit Shader。

133 DrawCall→1 DrawCall，CPU蒙皮↓80%+。与Animator共存(隐藏层SMR→AlwaysAnimate→GPU独立渲染)。配置不全自动回退标准SMR。

**已知GC问题** (Profiler实测 ~266KB/frame):
- `foreach(_meshData)` → Enumerator装箱 → 改for+List
- `GetComponentInChildren`每帧深搜 → Enemy.Awake缓存
- `smr.bones/bindposes`数组拷贝(~240KB) → Enemy.Awake缓存

### 3.5 Buff系统

15种Buff, 3种生命周期(非唯一/唯一/临时):

```
属性计算链(多级乘法器):
伤害 = base × LvMultiplier × BuffMultiplier × TemporaryMultiplier
HP上限 = baseHP × LvMult × MaxHPMult × AddedHp
```

核心Buff: Scope(瞄具), Laser(镭射), Grip(握把), Magazine(扩容), Adrenaline(HP<30%攻↑), LastStand(HP↓Attack↑), LifeSteal吸血, DronePower/IceBombPower 技能强化。

赌博系统: 5级概率(大吉5%×2^stack经验+无敌, 吉15%, 小吉30%, 不中50%)。3数字滚动画+定格弹跳。

---

## 四、第三方FPS框架集成

### 4.1 Infima Games Low Poly Shooter Pack

抽象-实现分层:
```
CharacterBehaviour(197行) → Character(~1050行)
WeaponBehaviour(213行)    → Weapon(~730行)
InventoryBehaviour(52行)  → Inventory(109行)
WeaponAttachmentManagerBehaviour(90行) → WAM(280行)
```

5类配件: Scope/Muzzle/Laser/Grip/Magazine → 支持随机选择+运行时装备。

### 4.2 集成方式

EventManager桥接: 移动端摇杆→EventManager→Character.SetAimingExternal/SetExternalFire等。Character暴露6个GenericProperty→PlayerUI自动绑定。

Buff对FPS参数修改链:
```
PlayerBuffManager.GetFireRate → Character.GetCurrentFireRate → Weapon.GetRateOfFire
PlayerBuffManager.GetMagazineCapacity → Weapon.GetAmmunitionTotal
```

---

## 五、性能优化

### 5.1 优化策略表

| 优化 | 方案 | 效果 |
|------|------|------|
| AI LOD | 4级距离降频(每/2/4/8帧) | 远敌CPU↓50-87% |
| 分批寻路 | 每帧1/5敌人+LOD跳过 | 寻路均摊至1/5 |
| 空间网格隔帧 | RebuildAll隔帧 | 2ms→1ms/帧 |
| GPU Skinning | ComputeShader+Instanced | 133DC→1 |
| 流场部分重建 | 每5帧1全+4部 | 重建↓80-90% |
| 流场查表 | 数组替代Physics | 零射线开销 |
| sqrMagnitude | 所有距离比较 | 避免sqrt() |
| Static数组复用 | RaycastHit[8]/Collider[32] | 零GC |
| Swap-Pop | 所有List删除 | O(1) |
| 对象池预热 | PrewarmWaveEnemies | 首波不卡 |

### 5.2 Profiler实测瓶颈

**TickActiveEnemies 22.96ms** (100+敌人):
- LOD阈值偏小→全部落近距→每帧全TickState
- CharacterController.Move每帧同步引擎调用
- transform.position C++ Interop开销

优化: 拉宽LOD(15→25/30→45/50→80) + 远距Move降频 + transform缓存。

---

## 六、技术难点与解决方案

### 难点1: 大规模实时寻路
**解决**: 自研FlowField全局BFS向量场+部分重建→边际成本≈0。

### 难点2: 移动端骨骼动画渲染
**解决**: GPU ComputeShader蒙皮+InstancedIndirect→133DrawCall→1。

### 难点3: 敌人群体行为
**解决**: SpatialGrid O(1)查询+Boids分离+振荡检测+切向绕行+流场查表避障(520行状态设计)。

### 难点4: 复杂Buff系统
**解决**: 分层乘法器+3种生命周期+Unique HashSet去重+临时效果乘算堆叠。

### 难点5: 跨模块通信
**解决**: EventManager 22事件中心+RunTimeContext Inject延迟绑定+GenericProperty响应式。

---

## 七、工作量统计

| 模块 | 文件 | 行数 | 复杂度 |
|------|------|------|--------|
| Framework | 8 | ~1,900 | ●●● |
| Enemy+State | 10 | ~2,300 | ●●●●● |
| FlowField+SpatialGrid | 3 | ~1,050 | ●●●●● |
| Player+BuffManager | 6 | ~2,000 | ●●●●● |
| UI | 14 | ~1,400 | ●●● |
| 其余(FSM/Wave/Combat/Editor/Tool) | 13 | ~890 | ●● |
| Shader | 1 | ~140 | ●●● |
| **自有代码合计** | **~55** | **~9,540** | — |
| 第三方FPS框架 | ~91 | — | — |
| Editor Toolbox | ~231 | — | — |

**数据资产**: 15波PortalWave + 15 Buff定义 + 6配置资产 + 1流场资产 + 3种丧尸 + 2技能预制体。

---

## 八、总结与展望

### 8.1 项目成果
- 完整移动端丧尸生存射击游戏(15波+功能齐全)
- 自研FlowField全域寻路+GPU骨骼蒙皮+完整Buff系统
- AI LOD/分批寻路/空间哈希/对象池等多层性能优化
- 第三方FPS框架解耦集成

### 8.2 个人技术成长
- 流场寻路(BFS vector field)算法实现
- GPU ComputeShader + InstancedIndirect编程
- 设计模式实战(状态/观察者/对象池/依赖注入)
- 移动端性能Profiler分析与优化

### 8.3 未来方向
- ECS架构进一步性能优化
- GPU端Boids分离计算
- 网络多人合作
- 武器/敌人类型扩展

---

*答辩文档 v3.0 | 2026-07-15*