# 丧尸生存射击游戏 — 完整技术文档

> **引擎**: Unity URP | **语言**: C# 9.0 | **目标平台**: Android/iOS | **文档版本**: v2.0 / 2026-07-15

---

## 目录

1. 项目总览与架构
2. 框架层详解
3. 敌人系统
4. 有限状态机
5. 追逐状态完整算法
6. 流场寻路系统
7. 空间哈希网格
8. GPU 骨骼蒙皮渲染
9. 玩家系统
10. Buff 系统完整设计
11. 波次系统
12. 技能系统
13. 赌博系统
14. 对象池系统
15. UI 系统
16. 事件系统设计
17. 定时器系统
18. 音频系统
19. 数据资产系统
20. 编辑器工具
21. 工具类与设计模式
22. 性能优化总论
23. 资源统计

---

## 1. 项目总览与架构

### 1.1 游戏玩法循环

```
生成丧尸波次 → 玩家射击消灭 → 获得经验升级 → 选择 Buff 增强自身
    ├─ 无人机自动索敌轰炸 (5s冷却)
    ├─ 冰弹自动索敌减速 (7s冷却)
    └─ 赌博小游戏获取额外增益
钟
15 波全部清除 → 胜利结算 | 玩家死亡 → 失败结算
```

### 1.2 目录结构

```
Assets/Scripts/
├── Framework/            # 8个核心管理器
│   ├── GameManager.cs        [-100执行顺序] 主管理器
│   ├── EventManager.cs       事件中心 (22个事件)
│   ├── EnemyManager.cs       敌人管理+AI LOD+分批寻路
│   ├── WaveManager.cs        波次生成调度
│   ├── UIManager.cs          UI桥接层
│   ├── AudioManager.cs       音频管理
│   ├── TimeManager.cs        [-90] 统一定时器
│   └── RunTimeContext.cs     运行时依赖注入
├── Enemy/                # 5个敌人组件
│   ├── Enemy.cs              ~490行 伤害/攻击/死亡/GPU集成
│   ├── EnemyMovement.cs      CC移动+避障参数
│   ├── EnemyAnimator.cs      动画状态封装
│   ├── EnemyBuff.cs          属性乘法器
│   └── EnemyGPUSkinningManager.cs  GPU蒙皮管理(~350行)
├── EnemyState/           # 5个敌人子状态
│   ├── EnemyBirthState.cs
│   ├── EnemyChaseState.cs    最复杂状态(~520行)
│   ├── EnemyAttackState.cs
│   ├── EnemyDeadState.cs
│   └── AnimatorTranslate.cs
├── FSM/                  # 4个状态机文件
│   ├── IState.cs
│   ├── StateMachine.cs
│   ├── EnemyState.cs
│   └── EnemyStateMachine.cs
├── FlowField/            # 3个寻路文件
│   ├── FlowField.cs          核心寻路算法(~835行)
│   ├── FlowFieldAsset.cs     Bake资产
│   └── SpatialGrid.cs        空间哈希分桶
├── Player/               # 6个玩家文件
│   ├── Player.cs             ~620行
│   ├── PlayerBuffManager.cs  ~1040行 Buff核心
│   ├── AreaDamageSkill.cs    ~230行 无人机/冰弹弹体
│   ├── PlayerLevelExperienceAsset.cs
│   ├── PlayerBuffPoolAsset.cs
│   └── PlayerBuffConfigAsset.cs
├── Combat/ProjectilePool.cs  # 通用对象池
├── UI/                   # 14个UI文件
├── Wave/SpawnPortal.cs   # 传送门生成器
├── Editor/FlowFieldBakerEditor.cs  # 流场烘焙工具
├── Interface/IDamage.cs
└── Tool/LazySingleton.cs, GenericProperty.cs
```

### 1.3 设计理念

| 设计模式 | 应用处 | 目的 |
|---------|--------|------|
| **Singleton** | GameManager, EventManager, TimeManager, RunTimeContext | 全局唯一访问点 |
| **Observer** | EventManager + GenericProperty | 模块解耦，事件驱动 |
| **State** | EnemyStateMachine (4状态) | 敌人行为清晰可控 |
| **ObjectPool** | ProjectilePool, EnemyManager | 减少GC和Instantiate |
| **Spatial Hashing** | SpatialGrid | O(1)邻居查询 |
| **Dependency Injection** | RunTimeContext.Inject* | 循环依赖解耦 |
| **Strategy** | FlowField vs DirectMovement | 寻路策略可切换 |

### 1.4 核心模块依赖图

```
        ┌──────────────┐
        │  GameManager  │ ← 启动入口、流场初始化、帧率控制
        └──────┬───────┘
               │
  ┌────────────┼──────────────┐
  ▼            ▼              ▼
┌──────┐  ┌───────────┐  ┌──────────┐
│Event │  │RunTime    │  │Time      │
│Mgr   │  │Context    │  │Manager   │
└──┬───┘  └─────┬─────┘  └────┬─────┘
   │            │              │
   └────────────┼──────────────┘
                │
    ┌───────────┼───────────┐
    ▼           ▼           ▼
┌──────┐ ┌──────────┐ ┌──────────┐
│Player│ │Enemy     │ │Wave      │
│      │ │Manager   │ │Manager   │
└──┬───┘ └────┬─────┘ └──────────┘
   │          │
   │   ┌──────┴──────┐
   │   ▼              ▼
   │ FlowField    SpatialGrid
   │ (流场寻路)    (空间哈希)
   │
   └──→ ProjectilePool/Drone/IceBomb
```

---

## 2. 框架层详解

### 2.1 GameManager (181行)

执行顺序 `[-100]`，最早运行。

**初始化**:
```
Start():
  Application.targetFrameRate = 120
  订阅 GamePause/GameResume/BeforeDemoRestart
  InitCharacter() → FindGameObjectWithTag("Player")
  InitFlowField() → FlowField.Initialize(FlowFieldAsset)
  设置流场目标为玩家位置
```

**Update循环**:
```
隔帧 SpatialGrid.RebuildAll() (133敌人约2ms，隔帧减半)
FPS统计 (每0.25s更新)
FlowField.SetTarget(player.position) (每帧)
```

**关键方法**: `WarmupFlowField()` 热机预初始化、`IsFlowFieldReady` 状态查询、`GetRandomTip()` 加载界面提示。

### 2.2 EventManager (140行)

全局事件中心，22个事件，全部解耦发布者与订阅者。

**事件分类**:

| 类别 | 事件 | 发布者→订阅者 |
|------|------|-------------|
| 战斗 | `Fire`, `Reload`, `Aim`, `MoveInput` | 输入系统→武器/角色 |
| 伤害 | `OnAttackedAction(float)` | Enemy攻击检测→Player |
| 经验 | `AddExper(float)` | Enemy.Death→Player |
| Buff | `TriggerBuff(int)`, `LevelUpBuffs`, `LevelUpBuffsFinished` | UI↔Player |
| 流程 | `GamePause`, `GameResume`, `BeforeDemoRestart` | UI→GameManager/EnemyManager/WaveManager |
| 赌博 | `RequestGambling`, `GamblingReady`, `GamblingFinished`, `GamblingRoundComplete` | UI↔Player |
| 大吉 | `GamblingGreatLuckStarted(float)`, `GamblingGreatLuckEnded` | Player→UI |
| 结算 | `SettleEvent(string)` | WaveManager/Player→UI |

**安全取消订阅**:
```csharp
// OnDisable中使用TryGetExistingInstance防止访问已销毁的Singleton
if (EventManager.TryGetExistingInstance(out EventManager em))
    em.SomeEvent -= Handler;
```

### 2.3 RunTimeContext (88行)

运行时依赖注入中心:
```csharp
Player Player; Character Character; WaveManager WaveManager;
GameObject PlayerObject;

// 变更事件
event Action<Player,Player> PlayerChanged;
event Action<Character,Character> CharacterChanged;
event Action<WaveManager,WaveManager> WaveManagerChanged;

// 延迟绑定: 如果目标已设置则立即回调，否则等注册后再回调
void InjectPlayer(Action<Player> bind) { if (Player != null) bind(Player); }
void InjectCharacter(Action<Character> bind) { ... }
void InjectWaveManager(Action<WaveManager> bind) { ... }
```

### 2.4 TimeManager (133行)

统一计时器系统，执行顺序 `[-90]`。

```csharp
int AddTimer(float duration, Action callback, bool useUnscaledTime=false);
int AddLoopTimer(float interval, Action callback, bool useUnscaledTime=false);
bool RemoveTimer(int timerId);
void ClearTimers();
```

**安全添加**: Update遍历中标记 `_isUpdating`，回调添加的计时器暂存 `_pendingAddTimers`，遍历结束再合并。

**使用场景**: 技能冷却(循环)、临时Buff过期(一次性)、赌博大吉过期、敌人减速过期、死亡动画延迟回池。

---

## 3. 敌人系统

### 3.1 Enemy核心 (489行)

```csharp
[RequireComponent(typeof(EnemyMovement))]
[RequireComponent(typeof(EnemyAnimator))]
public class Enemy : MonoBehaviour, IDamage
```

**属性乘法器架构**:
```
基础值(Inspector) → EnemyBuff.GetXxx(base) → 运行时值
  MaxHP = base × MaxHPMultiplier
  AttackDamage = base × AttackDamageMultiplier
  MoveSpeed = base × MoveSpeedMultiplier × TemporarySlowMultiplier
```

**攻击检测**: 动画事件帧 → `Physics.SphereCastNonAlloc` (static数组复用，8元素) → 判断collider是否是玩家子对象。

**自爆 (BoomAfterAttack)**: 攻击动画结束 → Stop移动 → Animator.SetBoom → 动画事件触发爆炸粒子+ `OverlapSphere` 检测 → 粒子结束回池。

**死亡流程**: `TakeDamage` → `_currentHP<=0` → `Die()` → `AddExperience` → `PlayDead` → `ScheduleReleaseToPool(DeadDestroyDelay)`。

**GPU Skinning集成**: OnEnable时检测 `EnemyGPUSkinningManager`，存在则隐藏SMR(GPUSkinningHidden层+关闭阴影)，注册GPU管理器，Animator设为AlwaysAnimate。

### 3.2 EnemyMovement (142行)

基于CharacterController。参数: moveSpeed=3.5, rotationSpeed=10, separationRadius=2.5m, separationForce=4, hardPushDistance=1.0m。

**Move**: 计算velocity → `CC.Move(motion * dt)` → `ClampYToZero()` → `FaceDirection` (RotateTowards, 角速度=rotationSpeed×60×dt)。

### 3.3 EnemyAnimator (135行)

动画参数: ChaseState(Float), Boom(Bool)。动画状态: Dead/GeiHit。

GPU兼容: `_animator.cullingMode = AlwaysAnimate`。

动画事件回调: Attack → enemy.OnAttackAnimationEvent, Dead → 记录时长+状态机切换, Boom → 自爆处理, Birth/GetHit退出 → 切换到chaseState。

### 3.4 EnemyBuff (70行)

**波次成长**: waveIndex = waveNumber-1, 乘法器累乘 `pow(growthPerWave, waveIndex)`:
- HP: ×1.13^waveIndex (+13%/波)
- 攻击: ×1.18^waveIndex (+18%/波)
- 经验: ×1.10^waveIndex (+10%/波)
- 移速: ×1.04^waveIndex (+4%/波)

可通过 `EnemyBuffConfigAsset` ScriptableObject自定义。

---

## 4. 有限状态机

### 4.1 类层次

```
IState (接口)
  └── StateMachine (抽象类: ChangeState/Update/NavUpdate)
        └── EnemyStateMachine (4状态: Birth/Chase/Attack/Dead)
              └── EnemyState (抽象: 便捷引用enemy/movement/animator)
                    ├── EnemyBirthState
                    ├── EnemyChaseState (~520行)
                    ├── EnemyAttackState
                    └── EnemyDeadState
```

### 4.2 状态转换

```
对象池取出 → Birth(计时结束) → Chase ←→ Attack
                                    │        │
                                    ▼        │
目标离开攻击范围 ←────────────────────┘        │
                                              │
HP<=0(任意状态) → Dead → 延迟回池
```

### 4.3 双通道更新

```
TickActiveEnemies (每帧+AI LOD降频):
  enemy.TickState() → stateMachine.Update() → 攻击检测+动画+移动

TickNavigationBatch (分批: 每帧处理1/5敌人):
  enemy.TickNavigation() → stateMachine.NavigationUpdate() → ComputeSteering
```

**设计理由**: 攻击检测必须每帧(体验)，寻路计算开销大(200enemies×BFS+分离)，分批+距离LOD降频优化。

---

## 5. 追逐状态完整算法 (518行)

### 5.1 每帧移动

```
Update():
  检查目标是否在攻击范围 → ChangeState(attackState)
  更新目标引用(每0.3s)
  计时器递减(orbitLockTimer, lateralAssistTimer)
  翻转窗口计时: 超过0.6s未翻转→重置

  GetMovementDirection(targetPos):
    if hasCachedMoveDirection:
      return ConstrainByObstacle(cachedDirection)  // 使用含分离/避障的缓存方向
    else:
      return GetLiveChaseDirection(targetPos)       // 流场轻量追击

  movement.Move(moveDir) / movement.Stop()
```

### 5.2 分批寻路 (ComputeSteering)

```
NavigationUpdate() 每批:

1. 流场方向: FlowField.TryGetFlowDirection(pos) → flowDir
   不可达时: flowDir = (target-pos).normalized

2. Boids分离: SpatialGrid.QueryNeighbors + 排斥力计算
   近距离: force += awayDir × HardPushForce × t² (平方衰减)
   远距离: force += awayDir × SeparationForce / dist
   限制: clamp(force, maxSepForce=6)
   LimitBackwardSeparation: 限制向后分离(最多-0.35分量)

3. 流场查表避障: FlowField.GetObstacleAvoidanceBias()
   → 检测前方/左前45°/右前45°阻挡→偏转值

4. 切向绕行: nearTarget(AttackRange+SeparationRadius)或radialConflict时
   ChooseOrbitSide: 流场查表检测左右→选不被挡的一侧→实例ID奇偶fallback
   绕行方向锁定(0.8s)防止频繁切换
   无进度检测(0.8s没靠近)→削弱绕行权重

权重混合:
  totalForce = flowDir × flowWeight(0.1~1.0, 近递减)
             + separation × separationWeight(1.8~3.0, 近递增)
             + avoidance × 1.6
             + lateral × lateralWeight(0~1.1)
  无安全力时校正回流场方向
  到达减速: dist < AttackRange×2 → speed × (dist/(2×atkRange))

5. 振荡检测:
   if Dot(prevSteer,steer) < -0.6 (翻转>126°):
     flipCount++
     if flipCount>=3 in 0.6s: 激活lateralAssist + 偏向flowField打破死循环

6. 平滑: SmoothDamp(smoothedDir, steer, 0.15s)

7. 缓存: _cachedMoveDirection = ConstrainByObstacle(smoothedDirection)
```

---

## 6. 流场寻路系统 (835行)

### 6.1 算法原理

将地图划分为均匀网格，从目标格做BFS计算每个格到目标的代价，每个格的流向指向代价最低的邻居。所有敌人共享同一个流场，每个敌人只需O(1)查表。

```
优势: 200个敌人边际成本≈0，远优于A* per-agent
```

### 6.2 数据结构

```csharp
static int _width, _height;
static float _cellSize = 1f;
static Vector2 _origin;
static bool[] _blockedCells;        // 障碍标记(来自Bake)
static int[] _costs;               // -2=障碍, -1=不可达, 0=目标, >0=距离
static Vector2[] _flowDirections;   // 每格流向向量
static bool[] _dirtyCells;
static Queue<Vector2Int> _bfsQueue;    // 复用,容量4096
static Vector2Int[] _neighbors = 8方向(正交10,对角14);
```

### 6.3 全量重建

```
ResetCosts() → 目标格cost=0,入BFS → BFS扩展8方向:
  stepCost = diagonal ? 14 : 10
  if newCost < cost[neighbor]: 更新并入队
→ 遍历所有格计算流向: 指向cost最小的邻居
```

### 6.4 部分重建 (核心优化)

```
每5次目标移动: 1次全量+4次部分

部分重建:
  目标格没变 → 直接return
  仅在新cost < 旧cost的格子上BFS扩展
  流向做全量重算(O(N)但每格只8次比较，极轻量)
  
省去ResetCosts和大部分BFS
实际耗时约全量的10-20%
```

### 6.5 避障查询API

| 方法 | 复杂度 | 用途 |
|------|--------|------|
| `TryGetFlowDirection` | O(1) | 获取流向 |
| `IsWalkable` | O(1) | 是否可行走 |
| `IsDirectionBlocked` | O(steps) | 方向阻挡检测 |
| `GetObstacleAvoidanceBias` | O(steps) | 前方/左前/右前阻挡 |
| `ProjectDirectionByObstacle` | O(1) | 贴墙滑动投影 |
| `TryGetRandomReachablePosition` | O(N) | 圈内随机可走位置 |

---

## 7. 空间哈希网格 (165行)

**设计**: CellSize=4m, Hash=`(x<<16)^(z&0xFFFF)`。

**4个数据结构**: `Dictionary<int,List<Enemy>> _cells` + `List<Enemy> _allEnemies` + `Dictionary<Enemy,int> _enemyIndices` + `Dictionary<Enemy,int> _enemyCells`。

**增量重建**: 每帧/隔帧遍历，仅在敌人跨格时移动列表(O(1))。

**邻居查询**: 确定中心格→遍历周围3×3范围格→筛选距离<radius的存活敌人。

**删除**: swap-pop O(1)，更新被移动敌人的索引。

---

## 8. GPU骨骼蒙皮渲染 (349行)

### 8.1 架构

```
CPU侧:                              GPU侧:
LateUpdate:                         ComputeShader "CSSkinning":
  收集骨骼矩阵→CPU buffer              VertexCount × InstanceCount 并行
  BoneMatrices.SetData()              输入: RestPositions/Normals/UVs
  Dispatch("CSSkinning")                      BoneWeights/Indices/Matrices
  DrawMeshInstancedIndirect()         输出: SkinnedPositions/Normals/UVs
                                     ↓
每Mesh一个MeshSkinningData:          GPUSkinningLit Shader:
  ComputeBuffer×11                    顶点从StructuredBuffer读取
  Material (GPUSkinningLit)           蒙皮后数据
  DrawArgs (IndirectArguments)      → TransformToWorld → URP PBR
                                    阴影Pass已移除
```

### 8.2 与Animator共存

SMR不移除(Animator需要)但移到GPUSkinningHidden层+关阴影。`Animator.cullingMode=AlwaysAnimate`覆盖隐藏层Culling。GPU端: ComputeShader蒙皮→DrawMeshInstancedIndirect批量绘制。

### 8.3 回退机制

`_isReady = skinningCompute!=null && materialTemplate!=null`。配置不全时Enemy自动回退标准SkinnedMeshRenderer。

---

## 9. 玩家系统 (621行)

### 9.1 核心数据

```csharp
float _maxHP=100f; int _level=1; int _levelUpBuffChooseCount=3;
// 响应式属性(绑定UI):
GenericProperty<float> CurrentHP, ExperienceProgress, DroneCooldownProgress, IceBombCooldownProgress;
GenericProperty<int> Level;
// Buff管理器:
PlayerBuffManager _playerBuffManager;
```

### 9.2 经验升级

```
AddExperience(value):
  _experience += value × ExperienceMultiplier(大吉可2x/4x/8x)
  while _experience >= GetRequiredExperience(): _level++
  升级回血: HealFlat(该等级HP增长量)
  AddPendingBuffChoices(升级次数) → DrawLevelUpBuffs()
```

### 9.3 伤害

```csharp
TakeDamage(rawDamage):
  if !IsAlive || IsInvincible(大吉) return;
  finalDamage = rawDamage × LevelDamageReduction × DamageReductionMultiplier × TemporaryMultiplier
  CurrentHP -= finalDamage
  OnCurrentHpChanged → CheckAdrenaline(HP<30%触发)
```

### 9.4 技能冷却

Update中每帧: `progress = (Time.time - startTime) / duration` → `GenericProperty.Value = progress` → UI自动刷新冷却条。

---

## 10. Buff系统完整设计 (1039行)

### 10.1 Buff类型

**非唯一**(可重复选择): Hp, AttackMultiplier, DamageReduction, LifeSteal, DroneSkillPower, IceBombSkillPower

**唯一**(选后从池移除): Scope(瞄准镜+减晃动), Laser(镭射+提速), Grip(握把+减后座), Magazine(扩容), SkillUnlock, LastStand(HP↓Attack↑受伤↑), Adrenaline(HP<30%触发), Sprint

### 10.2 属性计算链

```
最终伤害 = baseDamage × (1+10%×(Level-1)) × AttackMultiplier × TemporaryAttackMultiplier
承受伤害 = rawDamage × (1-2%×(Level-1)) × DamageReductionMultiplier × TemporaryDmgReductionMultiplier
吸血 = dealtDamage × LifeStealPercent
HP上限 = baseHP × (1+5%×(Level-1)) × MaxHPMultiplier + AddedHp
武器: 射速×FireRateMultiplier | 晃动×SwayMultiplier | 后座×RecoilMultiplier | 弹匣+added
技能伤害 = configDamage × SkillPowerMultiplier × TemporarySkillPowerMultiplier
```

### 10.3 Buff选择流程

```
升级 → AddPendingBuffChoices → DrawLevelUpBuffs:
  抽取: BuffPoolAsset.GetRandomDifferentBuffs(count, excluded, manager)
    → 候选列表(过滤已唯一+不能抽取的)
    → Fisher-Yates洗牌 → 取前count → 去重
  → GetBuffNamesAndDescs → EventManager.SetLevelUpBuffs → UIManager

玩家选择 → TriggerBuff(index) → TryApplySelectedBuff:
  → ApplyBuff → TriggerBuff → switch(Kind) → 具体应用
  → Unique→usedUniqueBuffs.Add | Temporary→启动定时器 | Adrenaline→记录buffAsset
  → pendingCount-- → DrawLevelUpBuffs(递归)
```

### 10.4 临时Buff

支持: AttackMultiplier, DamageReduction, DroneSkillPower, IceBombSkillPower, LifeSteal。多临时效果乘算。

### 10.5 肾上腺素

获取buff→HP<30%时自动触发→创建2个临时效果(攻击+30%,受伤-30%)→持续buff.Duration秒→终身只触发一次。

### 10.6 背水一战(LastStand)

MaxHP×0.7, Attack×1.7, DamageReceived×1.7 — 高风险高回报。

---

## 11. 波次系统 (325行)

**数据**: 15个PortalWave ScriptableObject → 每个含SpawnPortal[]。

**流程**:
```
启动 → FirstWaveCountdown(5s) → SpawnNextWave:
  随机选SpawnPoint → Instantiate(SpawnPortal)
  portal.Init(enemyManager.SpawnEnemy, NotifyPortalFinished, count, ...)
  
SpawnPortal: while敌人未生成完+轮次<waveNumber:
  每轮 currentRoundCount个 → 加权随机选类型 → 
  GetRandomSpawnPosition(流场可达+胶囊体无碰撞+无附近敌人)
  → spawnCallback(prefab, pos, rot), 间隔0.5s → 等timeBetweenEnemyWaves

波结束: _activePortals==0 && !HasCurrentWaveEnemies → 最后波胜利/下波8s倒计时
```

**生成验证**: 流场可达性→Physics.CheckCapsule→SpatialGrid.QueryNeighbors。失败→环形扩展fallback。

---

## 12. 技能系统

### 12.1 环形扫描

```
ScanBestSkillTarget(owner, range, acquireRadius, aoeRadius):
  16方向环形SphereCast → 统计每方向存活敌人 → 找最多方向
  ChooseBestAoeCenter: 计算最佳落点(覆盖最多敌人的位置)
```

### 12.2 无人机 (默认5s冷却)

扫描→`ProjectilePool.Spawn(drone)`→抛物线飞行(0.6s,弧高2m)→落地爆炸→OverlapSphere伤害→回池。伤害=20×DroneSkillPower。

### 12.3 冰弹 (默认7s冷却)

同流程+额外: `enemy.ApplyTemporarySlow(0.5, 2.0)` 50%减速2s。伤害=8×IceBombSkillPower。

### 12.4 AreaDamageSkill弹体 (229行)

抛物线飞行→爆炸(隐藏模型+播放粒子+ApplyAreaEffect)→延迟1.5s→ReleaseToPool。

---

## 13. 赌博系统 (189行)

### 13.1 概率表

| 结果 | 概率 | 效果 |
|------|------|------|
| 不中 | 50% | 无效果 |
| 小吉 | 30% | 恢复30%HP |
| 吉 | 15% | 10s攻击+20%(临时Buff) |
| 大吉 | 5% | 20s×2^stack经验+无敌(可堆叠) |

### 13.2 动画

3个数字依次定格(间隔0.7s)，越晚滚动越慢(Lerp(0.03,0.25,progress))。定格: 3次闪烁→弹跳缩放(1.4→1.0, OutBack)→颜色变化(白→金→白)→等2s→callback。

### 13.3 大吉堆叠

连续中大吉: stack++ → 经验×2,×4,×8... → 只要stack>0就无敌。每层独立20s计时器。

### 13.4 赌博作为Buff替代

消耗1次Buff选择机会。流程: RequestGambling→GetGamblingResult→GamblingReady→UI动画→callback→GamblingRoundComplete→DrawLevelUpBuffs(下一轮Buff选择)。

---

## 14. 对象池系统

### 14.1 通用池 (167行)

```csharp
ProjectilePool: Dictionary<GameObject, Queue<GameObject>>, MAX=30
Spawn→Dequeue/Instantiate→SetActive(true)→ResetTrail→Rigidbody.WakeUp
Release→ResetPhysics→ResetTrail→SetActive(false)→入池(>30则Destroy)
Prewarm(count)→预创建入池
PooledProjectile组件: Prefab引用(回池反向查找)+ResetPhysics+ResetTrail
```

### 14.2 敌人池 (EnemyManager内部)

`Dictionary<GameObject, Queue<Enemy>>` MAX=100/种。

**延迟回池**: 死亡动画播完后才回池(`ScheduleEnemyRelease`→Coroutine延迟→`ReleaseEnemy`)

两种池独立管理，Root GameObject不同。

---

## 15. UI系统

### 15.1 UIManager (347行)

桥接层: BuffChoose/Gambling/Settle面板引用 + 波次文本 + GenericProperty数据绑定。

生命周期: LevelUpBuffs→GamePause→BuffChoose.PlayOpen | LevelUpBuffsFinished→GameResume | SettleEvent→Settle.PlayOpen+GamePause。

### 15.2 动画

**波次倒计时** (DOTween): 显示(OutBack弹性滑入+缩放0.92→1.5+Punch), 隐藏(InBack滑出+缩放1.5→0.92)。

**面板打开** (UIPanelAnimator): Scale 0.88→1.0(OutBack,0.25s) + Alpha 0→1(0.16s)。

### 15.3 PlayerUI (197行)

8个GenericProperty绑定: HP/EXP/LV/子弹/枪支配件/冲刺/无人机冷却/冰弹冷却。通过RunTimeContext.Inject*延迟绑定。

### 15.4 BuffChoose (274行)

3个Buff选项。点击: 缩放1.1+金色(DOTween)→二次点击确认→`EventManager.SetBuffIndex`。赌博按钮→`EventManager.RequestGambling`。

---

## 16. 事件系统

22个事件的发布-订阅关系图:

```
Player ←OnAttackedAction─ Enemy攻击检测
Player ←AddExper─ Enemy.Death
UIManager ←LevelUpBuffs─ PlayerBuffManager →UIManager
Player ←TriggerBuff─ UIManager →Player
GameManager ←GamePause/Resume─ UIManager
GameManager+EnemyManager+WaveManager ←BeforeDemoRestart─ Settle
UIManager ←SettleEvent─ WaveManager(胜利)/Player(失败)
Player ←RequestGambling─ UIManager →Player
UIManager ←GamblingReady─ PlayerBuffManager →UIManager
```

---

## 17. 定时器系统 (133行)

自增ID分配，支持Loop/UnscaledTime。Update中遍历，安全添加机制(`_isUpdating`标记→暂存`_pendingAddTimers`→遍历结束合并)。

---

## 18. 音频系统 (206行)

AudioMixer: Master/BGM/SFX三组。场景音乐切换(SceneManager.sceneLoaded), PlayerPrefs持久化音量。

---

## 19. 数据资产

| 资产 | 说明 |
|------|------|
| EnemyBuffConfig | 敌人波次成长率 |
| FlowField | Bake后的流场障碍数据 |
| PlayerBuffConfig | 玩家等级成长+技能参数 |
| PlayerBuffPool | Buff可抽取池(15种) |
| PlayerLevel | 每级所需经验值 |
| TipDatabase | 加载界面随机提示 |
| PortalWave 0~14 | 15波敌人配置 |
| Buff/NotUnique/ (6个) | 非唯一Buff定义 |
| Buff/Unique/ (9个) | 唯一Buff定义 |

---

## 20. 编辑器工具

**FlowFieldBakerEditor** (86行): 自定义Inspector → [Bake From Current Scene]按钮 → `Physics.CheckBox`逐格检测障碍 → 保存到.asset。GameManager的OnDrawGizmos提供Scene视图可视化(网格线/障碍红方块/目标黄菱形/方向绿→蓝箭头/图例)。

---

## 21. 工具类与设计模式

**LazySingleton<T>** (77行): 线程安全双重检查锁+自动创建GameObject+`TryGetExistingInstance`安全获取+`OnApplicationQuit`防退出创建。

**GenericProperty<T>** (26行): 响应式属性，值变化时`OnValueChanged?.Invoke(value)` → UI自动刷新。

**IDamage**接口: `TakeDamage(float)` + `TakeDamage(float, Vector3)` → Player和Enemy统一入口。

---

## 22. 性能优化总论

### AI LOD距离分级

| 距离 | 状态更新 | 寻路更新 |
|------|---------|---------|
| 0-15m | 每帧 | 每批(不跳过) |
| 15-30m | 每2帧 | 每2轮 |
| 30-50m | 每4帧 | 每4轮 |
| >50m | 每8帧 | 每8轮 |

### 分批寻路: batchCount=5, 每帧1/5敌人, 5帧一轮完整寻路。

### 隔帧空间网格: RebuildAll约2ms, 隔帧执行→1ms/帧。

### 数据复用: static数组(无GC) + sqrMagnitude替代Distance + swap-pop O(1)删除。

### GPU Skinning合批: 133敌人→1个DrawCall + 1次ComputeShader Dispatch。阴影Pass移除。

### 流场部分重建: 每5次1次全量+4次部分→部分重建耗时约全量的10-20%。

### 对象池预热: PrewarmWaveEnemies避免首波Instantiate卡顿。

### 流场查表替代Physics: IsDirectionBlocked/GetObstacleAvoidanceBias/ProjectDirectionByObstacle均为数组查表,O(steps)无Physics开销。

---

## 23. 资源统计

| 模块 | 文件数 | 约行数 | 复杂度 |
|------|--------|--------|--------|
| Framework | 8 | ~1900 | ●●● |
| Enemy+State | 10 | ~2300 | ●●●●● |
| FSM | 4 | ~150 | ●● |
| FlowField+SpatialGrid | 3 | ~1050 | ●●●●● |
| Player+BuffManager | 6 | ~2000 | ●●●●● |
| UI | 14 | ~1400 | ●●● |
| Combat/Wave/Editor/Interface/Tool | 8 | ~600 | ●● |
| **Shaders** | 1 | ~140 | ●●● |
| **总计** | ~54 | ~9,540 | — |

**第三方依赖**: Infima Games Low Poly Shooter Pack(武器系统)、DOTween(动画引擎)、Dark UI(UI主题)、Low Poly FPS Lite(场景模型)、Nebula Skyboxes(天空盒)。

---

*文档基于项目完整源码 `e:/Github/test` 生成 | v2.0 | 2026-07-15*