# 丧尸生存射击游戏 — 完整技术文档

> **引擎**: Unity URP | **语言**: C# 9.0 | **目标平台**: Android/iOS | **文档版本**: v3.0 / 2026-07-15

---

## 目录

1. [项目总览与架构](#1-项目总览与架构)
2. [框架层详解](#2-框架层详解)
3. [敌人系统](#3-敌人系统)
4. [有限状态机](#4-有限状态机)
5. [追逐状态完整算法](#5-追逐状态完整算法)
6. [流场寻路系统](#6-流场寻路系统)
7. [空间哈希网格](#7-空间哈希网格)
8. [GPU骨骼蒙皮渲染](#8-gpu骨骼蒙皮渲染)
9. [玩家系统](#9-玩家系统)
10. [Buff系统完整设计](#10-buff系统完整设计)
11. [波次系统](#11-波次系统)
12. [技能系统](#12-技能系统)
13. [赌博系统](#13-赌博系统)
14. [对象池系统](#14-对象池系统)
15. [UI系统](#15-ui系统)
16. [事件系统设计](#16-事件系统设计)
17. [定时器系统](#17-定时器系统)
18. [音频系统](#18-音频系统)
19. [数据资产系统](#19-数据资产系统)
20. [编辑器工具](#20-编辑器工具)
21. [工具类与设计模式](#21-工具类与设计模式)
22. [性能优化与Profiler分析](#22-性能优化与profiler分析)
23. [资源统计](#23-资源统计)

---

## 1. 项目总览与架构

### 1.1 游戏玩法

```
生成丧尸波次 → 玩家射击消灭 → 获得经验升级 → 选择Buff增强自身
    ├─ 无人机自动索敌轰炸 (5s冷却)
    ├─ 冰弹自动索敌减速   (7s冷却)
    └─ 赌博小游戏获取额外增益

15 波全部清除 → 胜利 / 玩家死亡 → 失败
```

### 1.2 目录结构

```
Assets/Scripts/
├── Framework/         # 8个核心管理器
│   ├── GameManager.cs       [-100执行顺序] 主管理器
│   ├── EventManager.cs      事件中心 (22个事件)
│   ├── EnemyManager.cs      敌人管理+AI LOD+分批寻路
│   ├── WaveManager.cs       波次生成调度
│   ├── UIManager.cs         UI桥接层
│   ├── AudioManager.cs      音频管理
│   ├── TimeManager.cs       [-90] 统一定时器
│   └── RunTimeContext.cs    运行时依赖注入
├── Enemy/             # 5个敌人组件
├── EnemyState/        # 5个敌人子状态
├── FSM/               # 4个状态机文件
├── FlowField/         # FlowField + SpatialGrid + FlowFieldAsset
├── Player/            # 6个文件 (Player/BuffManager/Skill/Config)
├── Combat/            # ProjectilePool 通用对象池
├── UI/                # 14个UI文件
├── Wave/              # SpawnPortal + SpawnPoint
├── Editor/            # FlowFieldBakerEditor
├── Interface/IDamage.cs
└── Tool/              # LazySingleton + GenericProperty
```

### 1.3 设计模式

| 设计模式 | 应用处 | 目的 |
|---------|--------|------|
| **Singleton** | GameManager, EventManager, TimeManager, RunTimeContext | 全局唯一访问点 |
| **Observer** | EventManager + GenericProperty | 模块解耦 |
| **State** | EnemyStateMachine (4状态) | 敌人行为控制 |
| **ObjectPool** | ProjectilePool + EnemyManager内池 | 减少GC |
| **Spatial Hashing** | SpatialGrid | O(1)邻居查询 |
| **Dependency Injection** | RunTimeContext.Inject* | 循环依赖解耦 |
| **Template Method** | FPS框架 *Behaviour 抽象基类 | 接口契约 |

### 1.4 核心模块依赖

```
        ┌──────────────┐
        │  GameManager  │ ← 启动入口
        └──────┬───────┘
   ┌──────────┼──────────┐
   ▼          ▼          ▼
EventMgr  RunTimeCtx  TimeMgr
   │          │          │
   └──────────┼──────────┘
              │
   ┌──────────┼──────────┐
   ▼          ▼          ▼
Player   EnemyMgr   WaveMgr
   │         │          │
   │   ┌─────┴─────┐    │
   │   ▼           ▼    │
   │ FlowField  SpatialGrid
   │
   └→ ProjectilePool/Drone/IceBomb
```

---

## 2. 框架层详解

### 2.1 GameManager (181行) | `[DefaultExecutionOrder(-100)]`

**职责**: 帧率控制(120fps)、流场初始化、FPS统计、暂停/恢复、Scene视图Gizmo可视化。

```csharp
// Update核心循环
Update():
  // 隔帧空间网格重建 (133敌人~2ms)
  _spatialGridSkipCounter++;
  if ((_spatialGridSkipCounter & 1) == 0)
    SpatialGrid.RebuildAll();

  UpdateFrameRate();  // 0.25s采样一次FPS

  // 流场目标每帧跟随玩家
  if (_flowFieldInitialized && _character != null)
    FlowField.SetTarget(_character.transform.position);
```

**关键方法**: `WarmupFlowField()` 热机预初始化、`IsFlowFieldReady` 状态查询、`GetRandomTip()` 加载界面随机提示。

### 2.2 EventManager (140行)

22个事件的集中式事件中心。按类别分组:

| 类别 | 事件 |
|------|------|
| 战斗 | `Fire`, `Reload`, `Aim`, `MoveInput`, `ExternalFire/Run/Sprint` |
| 伤害 | `OnAttackedAction(float)`, `AddExper(float)` |
| Buff | `TriggerBuff(int)`, `LevelUpBuffs(string[],string[])`, `LevelUpBuffsFinished` |
| 流程 | `GamePause`, `GameResume`, `BeforeDemoRestart`, `SettleEvent(string)` |
| 赌博 | `RequestGambling`, `GamblingReady`, `GamblingFinished`, `GamblingRoundComplete`, `GamblingGreatLuckStarted/Ended` |

安全取消订阅: `if (EventManager.TryGetExistingInstance(out var em)) em.SomeEvent -= handler;`

### 2.3 RunTimeContext (88行)

运行时依赖注入中心:
```csharp
Player Player; Character Character; WaveManager WaveManager;
GameObject PlayerObject;

// 延迟绑定: 目标已设置→立即回调; 否则等注册后回调
void InjectPlayer(Action<Player> bind);
void InjectCharacter(Action<Character> bind);
void InjectWaveManager(Action<WaveManager> bind);

// 变更事件: 携带新旧值
event Action<Player,Player> PlayerChanged;
```

### 2.4 TimeManager (133行) | `[DefaultExecutionOrder(-90)]`

```csharp
int AddTimer(float duration, Action callback, bool useUnscaledTime=false);
int AddLoopTimer(float interval, Action callback);
bool RemoveTimer(int timerId);
```

安全添加: Update遍历中 `_isUpdating=true`，回调添加的计时器暂存 `_pendingAddTimers`，遍历结束合并。使用场景: 技能冷却(循环)、临时Buff过期、赌博大吉过期、敌人减速过期、死亡延迟回池。

### 2.5 EnemyManager (458行)

核心职责: 敌人对象池、AI LOD、分批寻路、波次生命周期。

```csharp
// 4个敌人列表 + swap-pop O(1)删除
List<Enemy> _activeEnemies;           Dictionary<Enemy,int> _activeEnemyIndices;
List<Enemy> _navigationEnemies;       Dictionary<Enemy,int> _navigationEnemyIndices;
HashSet<Enemy> _currentWaveEnemies;   // 当前波敌人(波清判定)

// 双通道更新
TickActiveEnemies()     → 状态更新(每帧+AI LOD降频)
TickNavigationBatch()   → 寻路更新(分批: 每帧1/5敌人)
```

**AI LOD 距离分级**:
| 距离 | 状态更新 | 寻路更新 |
|------|---------|---------|
| 0-15m | 每帧 | 每批(不跳过) |
| 15-30m | 每2帧 | 每2轮 |
| 30-50m | 每4帧 | 每4轮 |
| >50m | 每8帧 | 每8轮 |

**分批寻路**: `_navigationBatchCount=5`→每帧处理1/5敌人，5帧完成一轮。

### 2.6 WaveManager (325行)

15个PortalWave ScriptableObject → SpawnPortal[] 逐批生成。生成位置验证: 流场可达→胶囊体无碰撞→空间网格无邻居。预热: 首波预Instantiate到对象池。

### 2.7 UIManager (347行)

桥接层: BuffChoose/Gambling/Settle面板 + 波次文本 + GenericProperty绑定。
DOTween动画: 波次倒计时显示(OutBack滑入+缩放+Punch)、隐藏(InBack滑出)。

---

## 3. 敌人系统

### 3.1 Enemy核心 (489行)

```csharp
[RequireComponent(typeof(EnemyMovement))]
[RequireComponent(typeof(EnemyAnimator))]
public class Enemy : MonoBehaviour, IDamage
```

**属性乘法器架构**:
```
Inspector基础值 → EnemyBuff.GetXxx(base) → 运行时值
  MaxHP = base × MaxHPMultiplier
  MoveSpeed = base × MoveSpeedMultiplier × TemporarySlowMultiplier
```

**攻击检测**: 动画事件帧 → `Physics.SphereCastNonAlloc` (static RaycastHit[8]复用) → 判断collider.IsChildOf(player)。

**自爆 (BoomAfterAttack)**: 攻击结束→Stop移动→Animator.SetBoom→爆炸粒子→OverlapSphere→回池。

**死亡**: `_isDying=true` → `AddExperience` → `PlayDead` → `ScheduleReleaseToPool(DeadDestroyDelay)`。

**GPU Skinning集成**: OnEnable检测→SMR移到GPUSkinningHidden层+关阴影→注册GPU管理器→Animator设为AlwaysAnimate。

### 3.2 EnemyMovement (142行)

CharacterController移动: velocity计算→`CC.Move(motion*dt)`→`ClampYToZero()`→`FaceDirection(RotateTowards)`。参数: moveSpeed=3.5, rotationSpeed=10, separationRadius=2.5m, separationForce=4, hardPushDistance=1m。

### 3.3 EnemyAnimator (135行)

动画参数: ChaseState(Float), Boom(Bool)。动画状态: Dead/GeiHit。GPU兼容: `AlwaysAnimate`。动画事件: Attack→伤害检测, Dead→时长+状态机切换, Boom→自爆, Birth/GetHit退出→转Chase。

### 3.4 EnemyBuff (70行)

波次乘法器累乘 `pow(growthPerWave, waveIndex)`:
- HP ×1.13^waveIndex (+13%/波)
- 攻击 ×1.18^waveIndex (+18%/波)
- 移速 ×1.04^waveIndex (+4%/波)
- 经验 ×1.10^waveIndex (+10%/波)

---

## 4. 有限状态机

### 4.1 类层次

```
IState (接口) → StateMachine (抽象) → EnemyStateMachine
  ├── EnemyBirthState
  ├── EnemyChaseState  (~520行, 最复杂)
  ├── EnemyAttackState
  └── EnemyDeadState
```

### 4.2 状态转换

```
对象池取出 → Birth(计时结束) → Chase ←→ Attack
                                     │
                              HP<=0   ▼
                                     Dead → 延迟回池
```

### 4.3 双通道更新

```
TickActiveEnemies (每帧+AI LOD) → TickState() → stateMachine.Update()
TickNavigationBatch (每帧1/5)   → TickNavigation() → NavigationUpdate()
```

---

## 5. 追逐状态完整算法 (518行)

### 5.1 每帧移动

```
Update():
  检查攻击范围 → ChangeState(attackState)
  更新目标引用(每0.3s)

  GetMovementDirection(targetPos):
    hasCachedMoveDirection → 使用缓存方向(含分离/避障)
    else → GetLiveChaseDirection(纯流场追击)

  movement.Move(moveDir) / movement.Stop()
```

### 5.2 分批寻路 (ComputeSteering)

```
NavigationUpdate():
  1. 流场方向: FlowField.TryGetFlowDirection
  2. Boids分离: SpatialGrid.QueryNeighbors + 平方衰减硬推开 + 限向后分离(≤0.35)
  3. 流场查表避障: GetObstacleAvoidanceBias (前方/左前45°/右前45°)
  4. 切向绕行: nearTarget或radialConflict时 → ChooseOrbitSide(流场查表左右→实例ID奇偶fallback)
     绕行方向锁定0.8s + 无进度检测→削弱绕行

  权重混合:
    totalForce = flowDir × (0.1~1.0近递减) + separation × (1.8~3.0近递增)
               + avoidance × 1.6 + lateral × (0~1.1)

  5. 振荡检测: Dot(prevSteer,steer)<-0.6 → flipCount≥3 in 0.6s → lateralAssist+偏向flowField
  6. 平滑: SmoothDamp(smoothedDir, steer, 0.15s)
  7. 缓存: _cachedMoveDirection = ConstrainByObstacle(smoothedDir)
  8. 到达减速: dist < AttackRange×2 → speed×dist/(2×atkRange)
```

---

## 6. 流场寻路系统 (835行)

### 6.1 核心数据结构

```csharp
int _width, _height; float _cellSize=1f; Vector2 _origin;
bool[] _blockedCells;        // 障碍标记(Bake)
int[] _costs;               // -2=障碍, -1=不可达, 0=目标, >0=距离
Vector2[] _flowDirections;   // 每格流向
Queue<Vector2Int> _bfsQueue;  // 复用, 容量4096
Vector2Int[] _neighbors = 8方向(正交10,对角14);
```

### 6.2 全量重建

ResetCosts → 目标格cost=0→BFS扩展8方向→diagonal=14/original=10→遍历所有格计算流向=指向代价最小邻居。

### 6.3 部分重建 (核心优化)

每5次目标移动: 1次全量+4次部分。部分重建: 仅BFS代价减小的格子，流向全量重算(O(N)但每格8次比较)。实际耗时约全量的10-20%。

### 6.4 避障查表API

| 方法 | 复杂度 | 用途 |
|------|--------|------|
| `TryGetFlowDirection` | O(1) | 获取流向 |
| `IsWalkable` | O(1) | 可行走判断 |
| `IsDirectionBlocked` | O(steps) | 方向阻挡检测 |
| `GetObstacleAvoidanceBias` | O(steps) | 前方/左前/右前阻挡 |
| `ProjectDirectionByObstacle` | O(1) | 贴墙滑动投影 |

### 6.5 FlowFieldAsset (68行)

ScriptableObject: WorldMin/Max, CellSize, AgentRadius, blockedCells[], BakedScene, BakedTime。

### 6.6 编辑器可视化

GameManager.OnDrawGizmos: 网格线+障碍红方块+目标黄菱形+方向绿→蓝箭头+左上角图例。

---

## 7. 空间哈希网格 (165行)

CellSize=4m, Hash=`(x<<16)^(z&0xFFFF)`。

**4数据结构**: Dict<int,List<Enemy>> + List<Enemy> + Dict<Enemy,int>×2。

**增量重建**: 隔帧遍历，仅跨格时移动列表项O(1)。邻居查询: 中心格→周围3×3范围→筛选。

**Swap-pop删除**: O(1)，更新被移动敌人索引。

---

## 8. GPU骨骼蒙皮渲染 (349行)

### 8.1 架构

```
CPU: 收集骨骼矩阵→SetData→Dispatch("CSSkinning")→DrawMeshInstancedIndirect
GPU: ComputeShader并行蒙皮(VtxCount×InstCount线程)
     → StructuredBuffer输出 → GPUSkinningLit Shader读取 → URP PBR
```

### 8.2 与Animator共存

SMR不删除(Animator需要驱动骨骼)但移到隐藏层+关阴影。Animator.cullingMode=AlwaysAnimate。GPU端独立渲染。

### 8.3 回退

`_isReady = skinningCompute!=null && materialTemplate!=null`。配置不全时自动回退标准SMR。

### 8.4 LateUpdate每帧流程

```
1. 收紧实例数组(移除null)
2. 收集骨骼矩阵: Instance×BonesPerInstance个 Matrix4x4
3. SetData上传 → ComputeShader.Dispatch
4. 材质绑定: SetBuffer
5. DrawArgs更新: [IndexCount, InstanceCount, 0, 0, 0]
6. DrawMeshInstancedIndirect (ShadowCastingMode.Off)
```

### 8.5 GPUSkinningLit Shader (141行)

顶点从StructuredBuffer读取: `bufferIdx = instanceID * _VertexCount + vertexID`。
原始顶点仅占位(满足DrawMeshInstancedIndirect布局要求)。片段: URP PBR + MixFog。ShadowCaster Pass已移除。

### 8.6 已知GC问题

LateUpdate中3个GC来源:
1. `foreach(var kvp in _meshData)` — Dictionary.Enumerator装箱
2. `GetComponentInChildren<SkinnedMeshRenderer>` — 每帧每Enemy深搜
3. `smr.bones` / `sharedMesh.bindposes` — 每次返回数组拷贝

优化方案: foreach→for+List缓存; Enemy.Awake缓存bones/bindposes; 预先缓存SMR引用。

---

## 9. 玩家系统 (621行)

### 9.1 核心字段

```csharp
float _maxHP=100; int _level=1; int _levelUpBuffChooseCount=3;
GenericProperty<float> CurrentHP, ExperienceProgress, DroneCooldown, IceBombCooldown;
GenericProperty<int> Level;
PlayerBuffManager _playerBuffManager;  // ~1040行
```

### 9.2 经验升级

```
AddExperience → _experience += value × ExperienceMultiplier
  while _experience >= GetRequiredExperience(): _level++
  HealFlat(升级HP增长) → AddPendingBuffChoices → DrawLevelUpBuffs
```

### 9.3 伤害

```
TakeDamage: IsAlive && !IsInvincible → 
  finalDamage = raw × LevelReduction × BuffReduction × TemporaryReduction
  CurrentHP -= finalDamage → CheckAdrenaline (HP<30%)
```

### 9.4 外部控制接口

Character.Start() 订阅EventManager桥接:
```csharp
EventManager.Fire += FireWeapon;
EventManager.Aim += SetAimingExternal;
EventManager.Reload += TryReload;
EventManager.ExternalFire += SetExternalFire;
EventManager.MoveInput += SetExternalMoveInput;
EventManager.ExternalRun += SetExternalRunning;
EventManager.ExternalSprint += SprintBackward;
```

Character暴露6个GenericProperty供UI绑定: IsAimingProp, IsRunningProp, IsFiringProp, CurrentWeaponSpreadProp, CurrentAmmoProp, GunAccessoryVisibleProp。

---

## 10. Buff系统完整设计 (1039行)

### 10.1 Buff类型

**非唯一**(可重复): Hp, AttackMultiplier, DamageReduction, LifeSteal, DroneSkillPower, IceBombSkillPower

**唯一**(选后从池移除): Scope(瞄具+减晃动), Laser(镭射+提速), Grip(握把+减后座), Magazine(扩容), SkillUnlock, LastStand(HP↓Attack↑受伤↑), Adrenaline(HP<30%触发), Sprint

### 10.2 属性计算链

```
伤害 = base × (1+10%×(Lv-1)) × AttackMultiplier × TemporaryMultiplier
受伤 = raw × (1-2%×(Lv-1)) × DamageReductionMultiplier × TemporaryMultiplier
吸血 = dealtDamage × LifeStealPercent
HP上限 = baseHP × (1+5%×(Lv-1)) × MaxHPMult × TemporaryMult + AddedHp
射速 = baseRate × FireRateMultiplier (Laser Buff)
晃动 = baseSway × SwayMultiplier (Scope Buff)
后座 = baseRecoil × RecoilMultiplier (Grip Buff)
弹匣 = baseCapacity + AddedMagazineCapacity (Magazine Buff)
```

### 10.3 Buff流程

```
升级 → DrawLevelUpBuffs → Fisher-Yates洗牌取3个 → UI展示+GamePause
选择 → TriggerBuff(index) → switch(Kind)→具体应用 → Unique入usedSet
     → Temporary启动计时器 → pendingCount-- → 递归DrawLevelUpBuffs
```

### 10.4 临时Buff

支持: AttackMultiplier, DamageReduction, DroneSkillPower, IceBombSkillPower, LifeSteal。多个临时效果乘算叠加。来源: 赌博吉(10s攻+20%)、大吉(20s无敌)、肾上腺素(有时限)。

### 10.5 肾上腺素

获取→HP<30%触发→创建2临时效果(攻+30%,受伤-30%)→持续buff.Duration秒→终身只触发1次。

### 10.6 背水一战 LastStand

MaxHP×0.7, Attack×1.7, DamageReceived×1.7 — 高风险高回报。

---

## 11. 波次系统 (325行) + SpawnPortal (120行)

**数据**: 15个PortalWave → SpawnPortal[] → 加权随机敌人类型 → 逐批生成(间隔0.5s)。

**生成验证**: 流场可达(排除前方60°锥形)→胶囊体无碰撞→空间网格无附近敌人→环形fallback。

**生命周期**: 首波5s倒计时 → SpawnNextWave → portal.Init → 波清回调 → 最后波胜利/下波8s倒计时。

**预热**: PrewarmWaveEnemies→预Instantiate到对象池, 避免首波卡顿。

---

## 12. 技能系统

### 12.1 环形扫描

16方向SphereCast → 统计每方向存活敌人 → 找最多方向 → 计算最佳AOE落点(覆盖最多敌人)。

### 12.2 无人机 (5s冷却)

扫描→ProjectilePool.Spawn→抛物线飞行(0.6s,弧高2m)→落地OverlapSphere伤害→回池。伤害=20×DroneSkillPower。

### 12.3 冰弹 (7s冷却)

同上+额外: enemy.ApplyTemporarySlow(0.5, 2.0)。伤害=8×IceBombSkillPower。

### 12.4 AreaDamageSkill弹体 (229行)

抛物线飞行→爆炸(隐藏模型+粒子+ApplyAreaEffect)→延迟1.5s→ReleaseToPool。OverlapSphere检测→TakeDamage+吸血+冰弹减速。

---

## 13. 赌博系统 (189行)

| 结果 | 概率 | 效果 |
|------|------|------|
| 不中 | 50% | 无 |
| 小吉 | 30% | 恢复30%HP |
| 吉 | 15% | 10s攻+20%(临时) |
| 大吉 | 5% | 20s×2^stack经验+无敌 |

动画: 3数字依次定格(间隔0.7s)，越晚滚越慢，定格闪烁3次→弹跳→金色→等2s→callback。

大吉堆叠: stack++→经验×2/×4/×8...→只要stack>0就无敌→每层独立20s计时器。

---

## 14. 对象池系统

### 14.1 通用池 (167行)

```csharp
ProjectilePool: Dict<GameObject, Queue<GameObject>>, MAX=30
Spawn→Dequeue/Instantiate→ResetTrail→WakeUp
Release→ResetPhysics→ResetTrail→入池(>30 Destroy)
Prewarm→预创建入池
PooledProjectile: Prefab引用+Rigidbody+Trail管理
```

### 14.2 敌人池 (EnemyManager内部)

Dict<GameObject, Queue<Enemy>>, MAX=100/种。延迟回池: 死亡动画播完才回池。

---

## 15. UI系统

### 15.1 UIManager (347行)

桥接BuffChoose/Gambling/Settle面板+波次文本+GenericProperty绑定。

### 15.2 动画

**波次倒计时** (DOTween): 显示(OutBack + 缩放0.92→1.5 + Punch) / 隐藏(InBack + 缩放1.5→0.92)。

**面板打开** (UIPanelAnimator): Scale 0.88→1.0(OutBack 0.25s) + Alpha 0→1(0.16s)。

### 15.3 PlayerUI (197行)

8个GenericProperty绑定: HP/EXP/LV/子弹/枪支配件/冲刺/无人机冷却/冰弹冷却。通过RunTimeContext.Inject*延迟绑定。

### 15.4 BuffChoose (274行) + Gambling (189行) + Settle (36行)

BuffChoose: 3选项+点击缩放/金色反馈→二次确认→TriggerBuff。赌博按钮→RequestGambling。Settle: 胜利/失败+退出/重开(加载Loading场景)。

---

## 16. 事件系统

22个事件流向: Enemy→AddExper→Player→DrawLevelUpBuffs→UIManager→GamePause→Player选Buff→TriggerBuff→Player→DrawLevelUpBuffs(递归)。安全取消订阅: TryGetExistingInstance。

---

## 17-21. 其余系统 (摘要)

**TimeManager** (133行): 自增ID+安全添加机制+Loop/Unscaled支持。

**AudioManager** (206行): Master/BGM/SFX三组+场景音乐切换+PlayerPrefs持久化。

**数据资产**: 15波PortalWave+15Buff定义(6非唯一+9唯一)+6配置资产+1流场资产。

**FlowFieldBakerEditor** (86行): 自定义Inspector→Bake按钮→Physics.CheckBox逐格→保存.asset。

**LazySingleton** (77行): 线程安全DCL+自动创建+TryGetExistingInstance+退出防护。

**GenericProperty** (26行): 值变化时自动OnValueChanged→UI自动刷新。

**IDamage**: TakeDamage(float) + TakeDamage(float, Vector3) → Player/Enemy统一入口。

---

## 22. 性能优化与Profiler分析

### 22.1 AI LOD距离分级

| 距离 | 状态更新 | 寻路更新 | 帧位掩码 |
|------|---------|---------|---------|
| 0-15m | 每帧 | 每批(不跳过) | `(frame&0)!=0`=false→不跳过 |
| 15-30m | 每2帧 | 每2轮 | `(frame&1)!=0` |
| 30-50m | 每4帧 | 每4轮 | `(frame&3)!=0` |
| >50m | 每8帧 | 每8轮 | `(frame&7)!=0` |

**实现**: `(frame & skipMask) != 0 → continue`。当前使用 `_navigationFrameCount` 做帧计数，存在当 `_navigationEnemies.Count==0` 时计数器停滞的边界Bug。

### 22.2 分批寻路调度

`_navigationBatchCount=5` → 每帧处理1/5敌人，5帧完成一轮。结合LOD: 远处敌人跳过更多轮→实际每帧寻路敌人远小于总数。

### 22.3 GC优化分析

**EnemyGPUSkinningManager.LateUpdate** (当前~266KB/frame):

| GC来源 | 原因 | 优化方案 |
|--------|------|---------|
| `foreach(var kvp in _meshData)` | Dict.Enumerator装箱 | for+List<MeshSkinningData>缓存 |
| `GetComponentInChildren<SMR>` × N | 每帧深搜+内部分配 | Enemy.Awake缓存CachedGPUSkinningSMR |
| `smr.bones` × N | 每次返回数组拷贝 (~200KB/frame) | Enemy.Awake缓存CachedGPUSkinningBones |
| `sharedMesh.bindposes` × N | 每次返回数组拷贝 | Enemy.Awake缓存CachedGPUSkinningBindPoses |

**每帧静态数组复用**: RaycastHit[8], Collider[8/32] → 零GC Alloc。

### 22.4 CPU瓶颈

**TickActiveEnemies 22.96ms** (实测Profile):

根因:
1. 100+敌人都落在近距LOD(阈值偏小) → 每帧全TickState
2. CharacterController.Move 每帧同步引擎调用(CollisionDetection开销)
3. transform.position每次C++ Interop

优化方向:
- 拉宽LOD阈值(15→25/30→45/50→80)
- ChaseState.Update中对远距敌人降频Move(每2帧1次)
- 缓存transform(CachedTransform)避免重复.property访问

### 22.5 数据复用与内存优化

| 技术 | 示例 | 收益 |
|------|------|------|
| static数组复用 | RaycastHit[8], Collider[32] | 零GC Alloc |
| swap-pop删除 | 所有List删除 | O(1)无数组搬移 |
| sqrMagnitude | 所有距离比较 | 避免sqrt() |
| 流场查表O(1) | 替代Physics.SphereCast | 无射线开销 |
| ComputeBuffer复用 | GPU端预分配 | 无GPU内存重分配 |
| 对象池预热 | PrewarmWaveEnemies | 避免首波卡顿 |
| DOTween复用 | Sequence.Join | 减少Tween创建 |
| GPU Skinning合批 | 133敌人→1 DrawCall | 渲染线程负载↓80%+ |
| 阴影Pass移除 | GPUSkinningLit无ShadowCaster | GPU阴影计算归零 |

### 22.6 流场部分重建

每5次目标移动: 1次全量+4次部分。部分重建: 仅BFS代价严格减小的格子 → 流向全量重算(O(N)每格8次比较极轻量)。实际耗时约全量的10-20%。

---

## 23. 资源统计

| 模块 | 文件 | 约行数 | 复杂度 |
|------|------|--------|--------|
| Framework | 8 | ~1,900 | ●●● |
| Enemy+State | 10 | ~2,300 | ●●●●● |
| FSM | 4 | ~150 | ●● |
| FlowField+SpatialGrid | 3 | ~1,050 | ●●●●● |
| Player+BuffManager | 6 | ~2,000 | ●●●●● |
| UI | 14 | ~1,400 | ●●● |
| Wave/Combat/Editor/Tool | 8 | ~600 | ●● |
| Shader | 1 | ~140 | ●●● |
| **合计** | **~54** | **~9,540** | — |

**第三方**: Infima Games FPS (91 .cs), DOTween, Editor Toolbox (231 .cs), Dark UI, LowPolyFPSLite。

---

*文档基于项目源码 e:/Github/test | v3.0 | 2026-07-15*