# Fire 丧尸生存射击游戏技术文档

> **文档定位**：面向开发、维护与技术评审
> **项目基线**：Unity 2022.3.48f1 / C# / URP 14.0.11
> **运行模式**：单机本地逻辑；仓库未发现 Photon、Mirror、Netcode、FishNet 等网络同步框架
> **证据标签**：`[代码事实]` ` [配置事实]` ` [运行实测]` ` [设计意图]` ` [待验证]`

## 1. 项目概览

Fire 是一款低多边形 FPS 丧尸波次生存游戏。玩家在 Demo 场景中使用枪械消灭从传送门生成的敌人，击杀获得经验，升级后选择 Buff，并使用 Drone、IceBomb 等自动技能。清除全部波次获得胜利，生命值归零进入失败结算。

### 1.1 基线与入口

- `[配置事实]` Unity 版本为 `2022.3.48f1`：`ProjectSettings/ProjectVersion.txt:1-2`。
- `[配置事实]` 主要依赖位于 `Packages/manifest.json:2-12`：AI Navigation 1.1.7、Input System 1.11.2、Post Processing 3.4.0、URP 14.0.11、TextMeshPro 3.0.7、Timeline 1.7.6、UGUI 1.0.0、Visual Scripting 1.9.4。
- `[配置事实]` Build Settings 入口为 `Assets/Scenes/Loading.unity` → `Assets/Scenes/Demo.unity`。
- Loading 负责提示、流场热机与异步进入 Demo；具体加载行为以 `Assets/Scripts/Framework/LoadingSceneManager.cs` 为准。

## 2. 目录与职责

```text
Assets/
├── Scripts/                         # 项目自研代码
│   ├── Framework/                   # GameManager、EventManager、WaveManager 等
│   ├── Player/                      # 玩家属性、Buff、技能
│   ├── Enemy/ EnemyState/ FSM/      # 敌人、状态机、动画与移动
│   ├── FlowField/                   # 流场与 SpatialGrid
│   ├── Combat/                      # ProjectilePool 与伤害桥接
│   ├── UI/                          # 摇杆、触摸、界面绑定
│   ├── Wave/                        # Portal、SpawnPoint
│   ├── Editor/ Tool/ Interface/     # 编辑器工具、通用工具、IDamage
├── Scenes/                          # Loading、Demo 及烘焙数据
├── Prefabs/                         # Player、Enemy、Portal、Drone、IceBomb
├── Data/                            # ScriptableObject 配置
│   ├── WaveData/                    # PortalWave 0-14，共 15 个波次
│   ├── PlayerData/                  # 等级、Buff 池和 Buff 定义
│   ├── EnemyData/                   # 敌人 Buff 配置
│   ├── MapData/                     # FlowField.asset
│   └── TipData/                     # Loading 提示
├── Infima Games/Low Poly Shooter Pack/ # 第三方 FPS 框架
├── Plugins/Demigiant/DOTween/       # 第三方动画/补间库
├── SharpPoly_Zombies/                # 丧尸模型、动画与资源
└── Dark UI/                         # UI 资源
```

## 3. 分层架构与生命周期

```text
Unity / Packages
    ├── Infima FPS Framework: Character、Movement、Weapon、Camera、Animation
    └── Assets/Scripts
        ├── Player / Buff / Skill
        ├── Enemy / FSM / Movement
        ├── FlowField / SpatialGrid
        ├── Wave / Spawn
        ├── Combat / Object Pool
        └── UI / Framework Managers
```

### 3.1 基础设施

- `GameManager` 使用高优先级执行顺序，设置目标帧率、查找 Player、初始化 FlowField、隔帧重建 SpatialGrid、刷新目标位置、暂停/恢复并在重启前清理池：`Assets/Scripts/Framework/GameManager.cs:6-153`。
- `EventManager` 集中广播开火、瞄准、移动、伤害、经验、暂停、Buff、赌博和结算事件：`Assets/Scripts/Framework/EventManager.cs`。
- `RunTimeContext` 注册 Player、Character、WaveManager，并提供延迟注入/变更通知：`Assets/Scripts/Framework/RunTimeContext.cs`。
- `TimeManager` 统一管理技能冷却、临时减速、Buff 过期和死亡延迟：`Assets/Scripts/Framework/TimeManager.cs`。

### 3.2 运行时顺序

1. Loading 场景初始化管理器并热机 FlowField。
2. 异步加载 Demo，Player/Character、WaveManager 等对象注册到 RunTimeContext。
3. GameManager 绑定玩家位置为流场目标；WaveManager 开始倒计时和生成波次。
4. EnemyManager 从敌人池取出对象，Enemy 重置属性并进入 Birth 状态。
5. 玩家攻击通过 `IDamage` 造成伤害；死亡敌人发放经验并延迟回池。
6. 玩家升级触发 Buff 选择；全部波次完成或玩家死亡触发结算。
7. 重启前通过 `BeforeDemoRestart` 清理 ProjectilePool、SpatialGrid、敌人和场景状态。

## 4. 玩家、战斗与成长

`Player` 实现 `IDamage`，持有 Infima `Character` 并管理生命、等级、经验、Buff、技能计时器：`Assets/Scripts/Player/Player.cs:5-84`。

- 生命值、经验和等级通过 `GenericProperty` 提供 UI 监听：`Player.cs:38-47`。
- 升级时从 `PlayerLevelExperienceAsset` 读取经验要求，生成 Buff 选项：`Player.cs:502-532`。
- `PlayerBuffManager` 负责伤害、射速、弹容量、吸血、最大生命、技能范围/伤害/冷却及配件效果；配置位于 `Assets/Data/PlayerData`。
- Drone 与 IceBomb 解锁后由 TimeManager 计时，使用环形 SphereCast 统计敌人密度，选择覆盖敌人更多的 AOE 中心，再从 ProjectilePool 生成：`Player.cs:138-182,280-499`。
- Player 通过 `character.RefreshCurrentWeaponSetup()` 使 Buff 修改的武器/附件参数生效：`Player.cs:53-64`。

## 5. 敌人与 FSM

`Enemy` 要求 `EnemyMovement` 和 `EnemyAnimator`，实现 `IDamage`，并持有 `EnemyStateMachine`：`Assets/Scripts/Enemy/Enemy.cs:5-72,245-253`。

状态链：

```text
池中取出 → Birth → Chase ↔ Attack
                       ↓
                    HP<=0
                       ↓
                    Dead → 延迟回池
```

- Birth 播放出生过程；Chase 使用流场方向与群体避让；Attack 由动画事件执行 SphereCast；Dead 播放死亡动画、发放经验并回池。
- 敌人攻击、自爆使用 `SphereCastNonAlloc`/`OverlapSphereNonAlloc` 的静态缓冲区，避免每次检测分配数组：`Enemy.cs:104-168`。
- Enemy 的临时减速通过 Buff 与 TimeManager 计时恢复：`Enemy.cs:395-429`。
- 死亡流程设置 `_isDying`、广播经验并根据动画时长安排回池：`Enemy.cs:431-494`。
- FSM 定义和状态转换见 `Assets/Scripts/FSM/EnemyStateMachine.cs` 与 `Assets/Scripts/EnemyState`。

## 6. FlowField、SpatialGrid 与性能机制

### 6.1 FlowField

编辑器将地图障碍烘焙为 `FlowFieldAsset`；运行时 `FlowField.Initialize` 读取资产，围绕玩家目标进行 BFS，敌人通过数组查询共享方向。实现位于 `Assets/Scripts/FlowField/FlowField.cs`，编辑器入口为 `FlowFieldBakerEditor.cs`。

- `[代码事实]` FlowField 支持目标设置、全量/部分重建、可达位置查询和障碍方向判断。
- `[设计意图]` 共享流场避免为每个敌人分别运行完整寻路。
- `[待验证]` 具体地图规模、重建耗时和可承载敌人数必须用 Profiler 在固定场景测量，不能由注释推导。

### 6.2 SpatialGrid 与分帧更新

`Assets/Scripts/FlowField/SpatialGrid.cs` 登记活动敌人并按网格查询邻居；EnemyManager 将状态更新和导航更新分离，并按距离 LOD/批次降低远处更新频率。其机制属于代码事实，实际帧率收益属于运行实测。

### 6.3 GPU Skinning

`EnemyGPUSkinningManager` 为满足配置时的敌人渲染路径；Enemy 在启用时隐藏标准 SkinnedMeshRenderer 的渲染层但保留 Animator 驱动，再注册 GPU 管理器：`Enemy.cs:255-297`。是否减少 DrawCall、CPU 蒙皮或 GC 必须用 Frame Debugger/Profiler 在指定平台实测；不能把历史文档数字直接当作当前保证。

## 7. 波次、刷怪与结算

`WaveManager` 读取 `PortalWave`，管理倒计时、Portal/SpawnPoint、生成数量和当前波集合；`EnemyManager` 负责对象池、活跃列表、导航批次和波次完成判定。刷怪应检查 FlowField 可达性、地面/碰撞和与玩家/已有敌人的距离；具体条件见 `Assets/Scripts/Framework/WaveManager.cs`、`EnemyManager.cs`、`Assets/Scripts/Wave`。

## 8. 数据驱动与扩展

- 新增波次：复制 `Assets/Data/WaveData` 中的 PortalWave，设置敌人类型、数量、生成节奏和增长配置，并在 WaveManager 引用链中确认顺序。
- 新增 Buff：创建 `PlayerBuffAsset`，配置唯一性、属性效果、描述和图标，加入 Buff Pool/Config。
- 新增敌人：制作包含 Enemy、EnemyMovement、EnemyAnimator 和碰撞体的 Prefab，配置 EnemyBuff，并接入 EnemyManager 池。
- 新增技能：实现 `AreaDamageSkill` 初始化/回收接口，预制体加入 ProjectilePool 预热与释放路径。
- 修改地图：重新执行 FlowField Bake，并确认 FlowFieldAsset 引用有效。

## 9. 故障排查

| 现象 | 检查项 |
|---|---|
| 敌人不移动 | FlowFieldAsset 是否设置并 Bake；目标是否注册；EnemyMovement/FSM 是否存在 |
| Loading 无法进入 Demo | Build Settings 场景顺序、场景名称和 LoadingSceneManager 引用 |
| 玩家不能开火 | Character、Inventory、Weapon、Muzzle、EventManager 和输入动作 |
| Buff 不改变武器 | PlayerBuffManager 回调、当前 Attachment、`RefreshCurrentWeaponSetup` |
| 技能不释放 | 技能是否解锁、Prefab 是否引用、Enemy LayerMask、ProjectilePool 是否初始化 |
| GPU 路径异常 | GPUSkinningHidden 层、ComputeShader、材质和回退配置 |
| 重启后对象残留 | BeforeDemoRestart、ProjectilePool.ReleaseAllActive、SpatialGrid.Clear |

## 10. 验证矩阵

1. 静态检查脚本、Prefab、Scene、SO 引用和 Build Settings。
2. Unity Console 无编译错误/Missing Script。
3. Play Mode 验收 Loading→Demo、移动/视角/瞄准/开火/换弹/切枪、波次、升级、技能、胜负和重启。
4. Profiler 固定地图、设备、分辨率、质量和敌人数，记录 Frame Time、CPU/GPU、GC Alloc、导航耗时、活跃对象和池数量。
5. 标注当前限制：移动端真实设备性能、动态障碍重建、GPU 路径配置完整性和高密度敌群边界均需单独验证。
